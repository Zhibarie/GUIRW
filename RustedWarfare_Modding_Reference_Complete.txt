====================================================================================================
RUSTED WARFARE - UNIT MODDING REFERENCE
Extracted from: Rusted_Warfare__Unit_Modding_Reference.xlsx (Sheet: 1.16 - Latest Version)
Total unique reference codes: 1168
====================================================================================================


====================================================================================================
SECTION: [core]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  name:                                                 string                        Defines the unit raw name, game uses it to identify as a unique name. (This is not displayed in-game)
    EXAMPLE: name: customTank1

  mass:                                                 int                           The 'weight' of the unit, defines how it collides with other units, a greater value means it's tougher to push.
    EXAMPLE: mass: 3000

  radius:                                               int                           Circular area around the unit that makes it selectable. (mouse click/screen touch)
    EXAMPLE: radius: 20

  price:                                                int / price                   The unit cost from builders/buildings. Defaults to credits if no resource type is used
    EXAMPLE: price: 500, gold=5, stone=10

  maxHp:                                                int                           The max health for the unit. (will spawn with this value).
    EXAMPLE: maxHp: 200

  buildSpeed:                                           float / s                     Time it takes to build the unit. (may multiply with builder speed)
    EXAMPLE: buildSpeed: 3s

  class:                                                string                        Reserved for future use, must be CustomUnitMetadata by default.
    EXAMPLE: class: CustomUnitMetadata

  techLevel:                                            int                           Defines the Tech Level of the unit, there're 3 levels and each will appear in a different color in the GUI.
    EXAMPLE: techLevel: 1

  altNames:                                             string(s)                     Comma separated list of names. Like name but lower priority, useful for multiple optional mods.
    EXAMPLE: altNames: custTank1, customTank1, cTank1

  strictLevel:                                          float                1.13.3   Defaults to 0. 1 = Errors if keys are duplicated. Add to "all-units.template" in root to apply to all units.
    EXAMPLE: strictLevel: 1

  isBio:                                                bool                          Choose whether the unit is bioligical or not, affects sound and splat (unless hideScorchMark:true)
    EXAMPLE: isBio: true

  isBug:                                                bool                          Changes some death defaults, and sort order in Sandbox.
    EXAMPLE: isBug: false

  isBuilder:                                            bool                 1.13     Normally required if this unit places buildings. Defaults to [ai]useAsBuilder.
    EXAMPLE: isBuilder: true

  streamingCost:                                        price                1.15     Like price but paid for overtime while this unit is being queued or built. Construction or queue is paused if resources run out while building.
    EXAMPLE: streamingCost: gem=420

  switchPriceWithStreamingCost:                         bool                 1.15     Shortcut to set streamingCost to price value and clear price, add to all-units.template to quickly switch a mod over to streaming resources.
    EXAMPLE: switchPriceWithStreamingCost: true

  selfRegenRate:                                        float                         Passive self repair rate.
    EXAMPLE: selfRegenRate: 0.01

  maxShield:                                            int                           The max shield hitpoints of the unit. Can start with 0 hitpoints if startShieldAtZero:true.
    EXAMPLE: maxShield: 500

  startShieldAtZero:                                    bool                          Unit starts with a 0 hitpoints shield on created if true.
    EXAMPLE: startShieldAtZero: true

  shieldRegen:                                          float                         Passive shield regen rate.
    EXAMPLE: shieldRegen: 0.15

  energyMax:                                            float                         Defaults to 0. Energy that can be used as ammo for turrets, laser defense and actions.
    EXAMPLE: energyMax: 1

  energyRegen:                                          float                         Passive energy regen rate.
    EXAMPLE: energyRegen: 0.001

  energyStartingPercentage:                             float                         Sets the percentage of charged energy when the unit is first built.
    EXAMPLE: energyStartingPercentage: 0.5

  energyNeedsToRechargeToFull:                          float                         Disables weapons using energy after reaching zero till fully recharged if true.
    EXAMPLE: energyNeedsToRechargeToFull: true

  energyRegenWhenRecharging:                            float                         Regen rate while recharging.

  armour:                                               int                  1.13     Damage taken away from each hit. (not currently used in any vanilla units)
    EXAMPLE: armour: 6

  armourMinDamageToKeep:                                int                  1.13     Min damage to keep from received damage. Defaults to 1.
    EXAMPLE: armourMinDamageToKeep: 2

  borrowResourcesWhileAlive:                            price                1.13.3   Takes these resources when created and returns them when removed or destroyed.
    EXAMPLE: borrowResourcesWhileAlive: gold=10

  borrowResourcesWhileBuilt:                            price                1.15     Like borrowResourcesWhileAlive but doesn't take affect till built. Mostly useful for buildings like houses that have negative resources to add to the unit cap, etc.
    EXAMPLE: borrowResourcesWhileBuilt: supplyCap = -10

  generation_resources:                                 price                1.13.3   Income unit creates. (custom resource version)
    EXAMPLE: generation_resources: credits=5, gold=20

  generation_active:                                    logicBoolean         1.13.3   Disables generation_resources/credits when false. (logic_boolean)
    EXAMPLE: generation_active: if not self.hp(lessThan=100)

  generation_credits:                                   int                           Income unit creates. (credits only)
    EXAMPLE: generation_credits: 2

  generation_delay:                                     int                           How often generation_resources/credits is added. Defaults to 40. (changing not recommended)
    EXAMPLE: generation_delay: 40

  showInEditor:                                         bool                          Set to false to hide unit in Sandbox editor. (Defaults to true)
    EXAMPLE: showInEditor: false

  displayText:                                          LocaleString                  The unit name that the game shows to the player.
    EXAMPLE: displayText: Custom Tank

  displayText_{LANG}:                                   string               1.13     LANG = ISO 639-1 Code to show this text instead when game is in this language.
    EXAMPLE: displayText_es: Tanque Personalizado

  displayDescription:                                   LocaleString                  Unit description that the game shows to the player.
    EXAMPLE: displayDescription: -Fast movement\n-Light damage

  displayDescription_{LANG}:                            string               1.13     LANG = ISO 639-1 Code to show this text instead when game is in this language.
    EXAMPLE: displayDescription_es: -Movimiento rápido\n-Daño ligero

  displayLocaleKey:                                     string                        Translation file key for unit name and description.
    EXAMPLE: displayLocaleKey: units.mechArtillery

  displayRadius:                                        int                           Defaults to radius value. Set to show a larger or smaller selection circle UI on units.
    EXAMPLE: displayRadius: 20

  uiTargetRadius                                        int                           Defaults to displayRadius value. Radius used when attacking/reclaiming/etc this unit
    EXAMPLE: uiTargetRadius: 10

  shieldRenderRadius:                                   int                           Defaults is a little bigger than radius. Set to show a larger or smaller shield circle on units.
    EXAMPLE: shieldRenderRadius: 12

  shieldDisplayOnlyDeflection:                          bool                          Hide shield unless deflecting shot if true.
    EXAMPLE: shieldDisplayOnlyDeflection: true

  shieldDeflectionDisplayRate:                          float                         Defaults to 4. High value causes shield deflection to fade disappear faster.
    EXAMPLE: shieldDeflectionDisplayRate: 3

  showOnMinimap:                                        bool                 1.13.3   Defaults to true. Hide units on minimap if false.
    EXAMPLE: showOnMinimap: false

  showActionsWithMixedSelectionIfOtherUnitsHaveTag:     string(s)            1.13.3   Shows a merged action list if all units selected includes one of these tags. Useful for converted units.
    EXAMPLE: showActionsWithMixedSelectionIfOtherUnitsHaveTag: true

  showOnMinimapToEnemies                                bool                 1.14     Useful for stealth units
    EXAMPLE: showOnMinimapToEnemies: false

  isBuilding:                                           bool                          Defines if the unit is a building.
    EXAMPLE: isBuilding: true

  footprint:                                            ints                          Left, up, right, down. Tiles taken up which block unit movement. Defaults to 0,0,0,0  = 1 center tile.
    EXAMPLE: footprint: 0,0,1,1

  constructionFootprint:                                ints                          Tiles taken up for placement of other buildings. Defaults to 0,0,0,0 = 1 center tile.
    EXAMPLE: constructionFootprint: -1,-1,1,3

  displayFootprint:                                     ints                          Left, up, right, down. Only applies to buildings, just used for GUI. Defaults to footprint.
    EXAMPLE: displayFootprint: 0,0,1,1

  buildingSelectionOffset:                              int                           Defaults to 0. Adds or removes padding on the drawn selection rect in UI.
    EXAMPLE: buildingSelectionOffset: 4

  buildingToFootprintOffsetX:                           float                         Defaults to 10. Change the building position in the footprint on the X-axis.
    EXAMPLE: buildingToFootprintOffsetX: 4

  buildingToFootprintOffsetY:                           float                         Defaults to 10. Change the building position in the footprint on the Y-axis.
    EXAMPLE: buildingToFootprintOffsetY: 6

  placeOnlyOnResPool:                                   bool                          Normally used for extractors, forces building construction in a resource pool.
    EXAMPLE: placeOnlyOnResPool: true

  selfBuildRate:                                        float                         Rate unit builds itself when placed without a builder.
    EXAMPLE: selfBuildRate: 0.0008

  ignoreInUnitCapCalculation                                                 1.14     defaults to true for buildings otherwise false. Set to true to not count this unit in unit cap.
    EXAMPLE: ignoreInUnitCapCalculation: true

  copyFrom:                                             file(s) (ini)                 Uses unit data from another ini file as default for this unit, supports multiple files.
    EXAMPLE: copyFrom: ROOT:defaultTanks.template, tankT1.ini

  dont_load:                                            bool                          Do not load unit, and don't error on missing data. Can be useful when used with copyFrom.
    EXAMPLE: dont_load: true

  overrideAndReplace:                                   string(s)                     Overrides another unit with this unit. Build links and map positions to target unit will be replaced.
    EXAMPLE: overrideAndReplace: builder, combatEngineer

  onNewMapSpawn:                                        string               1.13.3   Values: emptyResourcePools_asNeutral, emptyOrOccupiedResourcePools_asNeutral, mapCenter_asNeutral, mapCenter_eachActiveTeam, spawnPoint_eachActiveTeam (1.15)
    EXAMPLE: onNewMapSpawn: spawnPoint_eachActiveTeam

  globalScale:                                          float                         Defaults to 1. Changing not recommended.
    EXAMPLE: globalScale: 2

  isLocked:                                             bool                          Disallow building of this unit. Can be used with overrideAndReplace to restrict units player can build.
    EXAMPLE: isLocked: true

  isLockedIfGameModeNoNuke:                             bool                 1.13     Disallows building of this unit if nukes are disabled during match setup.
    EXAMPLE: isLockedIfGameModeNoNuke: true

  experimental:                                         bool                          Tag unit as experimental. Affects zoomed out icon and end game stats.
    EXAMPLE: experimental: true

  stayNeutral:                                          bool                          Set to false to disable capture when unit is on the neutral team.
    EXAMPLE: stayNeutral: false

  createNeutral:                                        bool                 1.13     Set to true to always spawn the unit on the neutral team.
    EXAMPLE: createNeutral: true

  createOnAggressiveTeam:                               bool                          Set to true to always spawn the unit on aggressive teams on single player matches.
    EXAMPLE: createOnAggressiveTeam: true

  tags:                                                 string(s)            1.13     List of comma separated strings. Used to classify units, create special actions and balances.
    EXAMPLE: tags: tank, smallTank, piercingDamage

  defineUnitMemory                                                           1.15     Can define several variables for custom storage, unique for each unit. Allowed types: boolean, float/number, unit, string.
    EXAMPLE: defineUnitMemory: boolean nukeActive, boolean laserReady, float experience, unit nextTarget, unit homeBase, string customText

  fogOfWarSightRange:                                   int                           Sets number of tiles this unit can see through the fog of war. Defaults to 15.
    EXAMPLE: fogOfWarSightRange: 35

  fogOfWarSightRangeWhileNotBuilt                       int                  1.14     Fog of War range when unit/building is incomplete. Defaults to fogOfWarSightRange
    EXAMPLE: fogOfWarSightRangeWhileNotBuilt: 10

  softCollisionOnAll:                                   int                           Creates a soft collision effect when touching other units.
    EXAMPLE: softCollisionOnAll: 3

  disableAllUnitCollisions:                             bool                          Unit cannot collide with others if true.
    EXAMPLE: disableAllUnitCollisions: true

  isUnrepairableUnit:                                   bool                 1.13     No unit can repair this unit if true.
    EXAMPLE: isUnrepairableUnit: true

  isUnselectable:                                       bool                          If true unit cannot be selected. (includes AI players)
    EXAMPLE: isUnselectable: true

  isUnselectableAsTarget                                bool                 1.14     Defaults to isUnselectable. Can be used to create units that cannot be selected but can be targeted for attack, reclaim, etc
    EXAMPLE: isUnselectableAsTarget: false

  isPickableStartingUnit:                               bool                 1.13     If true, unit is added to dropdowns for starting unit in game setup menus.
    EXAMPLE: isPickableStartingUnit: true

  startFallingWhenStartingUnit:                         bool                 1.13     Unit will appear falling from skies when starting unit if true.
    EXAMPLE: startFallingWhenStartingUnit: true

  soundOnAttackOrder:                                   sound(s)                      List of sound names. Only one will be played on each attack order. Only .ogg and .wav formats.
    EXAMPLE: soundOnAttackOrder: tankAttackOrder1.ogg, tankAttackOrder2.ogg

  soundOnMoveOrder:                                     sound(s)                      List of sound names. Only one will be played on each move order. Only .ogg and .wav formats.
    EXAMPLE: soundOnMoveOrder: tankMoveOrder1.ogg, tankMoveOrder2.ogg

  soundOnNewSelection:                                  sound(s)             1.13.3   List of sound names. Only one will be played on each unit selection. Only .ogg and .wav formats.
    EXAMPLE: soundOnNewSelection: tankSelection1.ogg, tankSelection2.ogg

  canNotBeDirectlyAttacked:                             bool                          No unit can directly target this unit. If true this will also skip this unit in victory/defeat checks. If true, canNotBeDamaged is true by default
    EXAMPLE: canNotBeDirectlyAttacked: true

  canNotBeDamaged                                       bool                          Defaults to value of canNotBeDirectlyAttacked (be careful setting this without canNotBeDirectlyAttacked, as AI will attack forever)
    EXAMPLE: canNotBeDamaged: true

  canNotBeGivenOrdersByPlayer:                          bool                 1.13.3   If true unit will not take player or AI orders.
    EXAMPLE: canNotBeGivenOrdersByPlayer: true

  canOnlyBeAttackedByUnitsWithTags:                     strings(s)           1.13.3   List of tag strings, only units with these tags can directly target this unit.
    EXAMPLE: canOnlyBeAttackedByUnitsWithTags: piercingTank, powerfulTank

  disableDeathOnZeroHp                                  bool                 1.14     Setting to true allows unit to continue living even at 0 HP, useful for custom "death" action. Warning: If not used with an autoTrigger, etc units will attack this unit forever.
    EXAMPLE: disableDeathOnZeroHp: true

  allowCaptureWhenNeutralByAI                           bool                 1.15p9   When true, it lets to be captured on contact by AI as well. Defaults as false
    EXAMPLE: allowCaptureWhenNeutralByAI: true

  transportSlotsNeeded:                                 int                  1.13     Defaults to 1. Number of slots this unit uses up in a transport, experimentals are often set to 5.
    EXAMPLE: transportSlotsNeeded: 2

  maxTransportingUnits                                  int                           Number of slots this units has for transporting other units.
    EXAMPLE: maxTransportingUnits: 5

  transportUnitsRequireTag:                             string(s)            1.13     Only allows trasport of units that have one of these tags.
    EXAMPLE: transportUnitsRequireTag: smallTank, soldier

  transportUnitsRequireMovementType:                    movementTypes                 Only allows trasport of units that have one of these movement types.
    EXAMPLE: transportUnitsRequireMovementType: AIR, WATER

  transportUnitsBlockAirAndWaterUnits:                  bool                          Defaults to true. This unit can only transport LAND units if true.
    EXAMPLE: transportUnitsBlockAirAndWaterUnits: false

  transportUnitsEachUnitAlwaysUsesSingleSlot:           bool                          Defaults to false. Units in this transport occupy 1 slot always if true, ignoring transportSlotsNeeded.
    EXAMPLE: transportUnitsEachUnitAlwaysUsesSingleSlot: true

  transportUnitsKeepBuiltUnits:                         bool                          Makes built units stay inside transport instead of exiting it once ready if true.
    EXAMPLE: transportUnitsKeepBuiltUnits: true

  transportUnitsCanUnloadUnits:                         LogicBoolean                  Defaults to: if not self.isOverLiquid() and not self.isMoving(). This unit cannot unload units if false.
    EXAMPLE: transportUnitsCanUnloadUnits: false

  transportUnitsAddUnloadOption:                        bool                          Defines if unload button should be added to the unit menu
    EXAMPLE: transportUnitsAddUnloadOption: false

  transportUnitsUnloadDelayBetweenEachUnit:             float                1.13.3   Changes the delay it takes between each unit getting unloaded.
    EXAMPLE: transportUnitsUnloadDelayBetweenEachUnit: 12

  transportUnitsKillOnDeath:                            LogicBoolean         1.13     Defaults to true. If false transported units don't die when transport dies.
    EXAMPLE: transportUnitsKillOnDeath: if self.isOverLiquid()

  transportUnitsHealBy:                                 float                1.13     Rate to heal units that are being transported.
    EXAMPLE: transportUnitsHealBy: 0.1

  transportUnitsBlockOtherTransports:                   bool                          Defaults to true, if false this transports can hold other transports.
    EXAMPLE: transportUnitsBlockOtherTransports: false

  whileNeutralTransportAnyTeam:                         bool                 1.13.3   This unit can transport units of any team while neutral if true.
    EXAMPLE: whileNeutralTransportAnyTeam: true

  whileNeutralConvertToTransportedTeam:                 bool                          Converts this unit to transported team while neutral. Useful with whileNeutralTransportAnyTeam.
    EXAMPLE: whileNeutralConvertToTransportedTeam: true

  convertToNeutralIfNotTransporting:                    bool                          Reverts back this unit to neutral when unloaded. Useful with whileNeutralTransportAnyTeam.
    EXAMPLE: convertToNeutralIfNotTransporting: true

  transportUnitsOnTeamChangeKeepCurrentTeam:            bool                          Keeps transported units on their orginal team when this unit is converted if true.
    EXAMPLE: transportUnitsOnTeamChangeKeepCurrentTeam: true

  resourceRate:                                         float                         Used with canReclaimResources. Allows other teams to reclaim this unit. Normally used with neutral team. Use price to set what resources are gained.
    EXAMPLE: resourceRate: 100

  similarResourcesHaveTag:                              string(s)                     When this has been reclaimed harvester unit moves on to another resource with these tags.
    EXAMPLE: similarResourcesHaveTag: goldResource

  resourceMaxConcurrentReclaimingThis:                  int                  1.13.3   Defaults to unlimited. Set to restict how many units can reclaim this resource at the same time.
    EXAMPLE: resourceMaxConcurrentReclaimingThis: 3

  reclaimPrice:                                         int                           Like price but for resources. Useful for buildable resources.
    EXAMPLE: reclaimPrice: gold=1000

  canReclaimResources:                                  bool                          If true this unit can gather resources, useful with resourceRate.
    EXAMPLE: canReclaimResources: true

  canReclaimResourcesNextSearchRange:                   int                           Defines the resource search range of this unit when its main gathered resource runs out.
    EXAMPLE: canReclaimResourcesNextSearchRange: 100

  canReclaimResourcesOnlyWithTags:                      string(s)                     This unit is only allowed to gather resources with these tags.
    EXAMPLE: canReclaimResourcesOnlyWithTags: foodResource, goldResource

  canReclaimUnitsOnlyWithTags                           string(s)                     This is for reclaiming units, not for resources. See canReclaimResourcesOnlyWithTags
    EXAMPLE: canReclaimUnitsOnlyWithTags: reclaimable

  resourceReclaimMultiplier                             float                1.15     Multiplies the builder's reclaim speed. Different from the related key "nanoUnbuildSpeed"
    EXAMPLE: resourceReclaimMultiplier: 1.5

  canRepairUnitsOnlyWithTags                            string(s)                     Repairs units with the specified tags
    EXAMPLE: canRepairUnitsOnlyWithTags: vulnerable

  canRepairBuildings:                                   bool                          Can this can heal ally buildings (isBuilder:true is required)
    EXAMPLE: canRepairBuildings: true

  canRepairUnits:                                       bool                          Can this can heal ally units. (isBuilder:true is required), canRepairBuildings required for buildings.
    EXAMPLE: canRepairUnits: true

  autoRepair:                                           bool                          Automatically try and repair damaged units in nano range. (isBuilder:true is required)
    EXAMPLE: autoRepair: true

  nanoRange:                                            int                           Defaults to 85. Defines the unit building/repair/reclaim range.
    EXAMPLE: nanoRange: 110

  nanoRepairSpeed:                                      float                         Defaults to 0.2. Defines the unit nano repair/reclaim speed.
    EXAMPLE: nanoRepairSpeed: 0.01

  nanoBuildSpeed:                                       float                         Defaults to 1. Defines the unit nano building speed. (May multiply with target's buildSpeed)
    EXAMPLE: nanoBuildSpeed: 0.9

  nanoUnbuildSpeed:                                     float                1.15     How fast a builder reclaims an incomplete building (defaults to 1)
    EXAMPLE: nanoUnbuildSpeed: 1.4

  nanoReclaimSpeed:                                     float                         How fast a builder reclaims a normal unit (not a resource unit)
    EXAMPLE: nanoReclaimSpeed: 0.23

  nanoRangeForRepairIsMelee:                            bool                 1.13.3   Defines if this unit must touch its target to repair it.
    EXAMPLE: nanoRangeForRepairIsMelee: true

  nanoRangeForReclaimIsMelee:                           bool                          Defines if this unit must touch its target to reclaim it.
    EXAMPLE: nanoRangeForReclaimIsMelee: true

  nanoRangeForRepair:                                   int                           Defines a specific range for the repair action of this unit.
    EXAMPLE: nanoRangeForRepair: 60

  nanoRangeForReclaim:                                  int                           Defines a specific range for the reclaim action of this unit.
    EXAMPLE: nanoRangeForReclaim: 60

  nanoFactorySpeed:                                     float                         Defaults to 1. Multiplies the buildSpeed value of the created unit if this unit is a factory.
    EXAMPLE: nanoFactorySpeed: 1.2

  extraBuildRangeWhenBuildingThis:                      int                           Temporarily adds extra build range to builders to build this unit. Useful for water based buildings.
    EXAMPLE: extraBuildRangeWhenBuildingThis: 90

  builtFrom_{NUM}_name:                                 string(s)                     Useful if adding this unit to build to existing buildings. Like canBuild but in opposite direction.
    EXAMPLE: builtFrom_1_name: landFactory, airFactory

  builtFrom_{NUM}_pos:                                  float                         Order this build link appears in UI. Using canBuild instead is more recommended.
    EXAMPLE: builtFrom_1_pos: 0.1

  builtFrom_{NUM}_forceNano:                            bool                          Build as if this is a building if true. (even if it's a unit)
    EXAMPLE: builtFrom_1_forceNano: true

  builtFrom_{NUM}_isLocked:                             LogicBoolean         1.13.3   If true this unit cannot be built in this build link. (can be conditioned if logicBooleans are used)
    EXAMPLE: builtFrom_1_isLocked: if self.hp(lessThan=100)

  builtFrom_{NUM}_isLockedMessage:                      LocaleString                  Message shown when this build link is locked.
    EXAMPLE: builtFrom_1_isLockedMessage: -Needs more population

  exit_x:                                               float                         Where created or unloaded units appears from the transport or building. Defaults to 0.
    EXAMPLE: exit_x: 0

  exit_y:                                               float                         Where created or unloaded units appears from the transport or building. Defaults to 5.
    EXAMPLE: exit_x: 5

  exit_dirOffset:                                       float                1.13     Defaults to 180 for units and 0 for buildings. Defines the exit direction of created or unloaded units.
    EXAMPLE: exit_dirOffset: 140

  exit_heightOffset:                                    float                         Defaults to 0. Defines the height where created or unloaded units appears.
    EXAMPLE: exit_heightOffset: 16

  exit_moveAwayAmount:                                  float                         Defaults to 70. Defines the distance that created or unloaded units moves from this unit.
    EXAMPLE: exit_moveAwayAmount: 10

  exitHeightIgnoreParent                                bool                 1.14     Ignores parent height for exit height; useful for separating attachments with their parents for building
    EXAMPLE: exitHeightIgnoreParent: true

  dieOnConstruct:                                       bool                          Deletes this unit when it starts to build if true. (target building/unit likely will need selfBuildRate set)
    EXAMPLE: dieOnConstruct: true

  dieOnZeroEnergy:                                      bool                 1.13     Kills this unit if energy level reaches zero when true.
    EXAMPLE: dieOnZeroEnergy: true

  numBitsOnDeath:                                       int                           Defines the number of scattered bit fragments when this unit dies.
    EXAMPLE: numBitsOnDeath: 20

  nukeOnDeath:                                          bool                          Unit will spawn a nuke detonation built-in effect when dies if true.
    EXAMPLE: nukeOnDeath: true

  nukeOnDeathRange:                                     float                         Defines the nuke effect range when using nukeOnDeath.
    EXAMPLE: nukeOnDeathRange: 140

  nukeOnDeathDamage:                                    float                         Defines the nuke effect area damage when using nukeOnDeath.
    EXAMPLE: nukeOnDeathDamage: 2000

  nukeOnDeathDisableWhenNoNuke:                         bool                          Defaults to false. If true this unit will not explode with nuke when nukes are disabled in skirmish maps.
    EXAMPLE: nukeOnDeathDisableWhenNoNuke: true

  fireTurretXAtSelfOnDeath:                             turret ref                    Auto-shoot a specific turret when this unit dies.
    EXAMPLE: fireTurretXAtSelfOnDeath: turret_1

  explodeOnDeath:                                       bool                 1.13     Defaults to true. Disables the unit death explode built-in effect if false.
    EXAMPLE: explodeOnDeath: false

  explodeOnDeathGroundCollision:                        boolean                       Defaults to true. Disables the explode built-in effect on death when unit touches ground if false.
    EXAMPLE: explodeOnDeathGroundCollision: false

  explodeTypeOnDeath:                                   preset effects       1.14     options: verysmall, small, normal, large, largeUnit, building, buildingNoShockwaveOrSmoke, verylargeBuilding

  effectOnDeath:                                        effect(s) ref        1.13     Spawns built-in or custom effects when unit dies.
    EXAMPLE: effectOnDeath: shockwave, CUSTOM:pieces*3, CUSTOM:deathSound

  effectOnDeathGroundCollision:                         effect(s) ref                 Like effectOnDeath but when unit touches ground. Useful for flying units.
    EXAMPLE: effectOnDeathGroundCollision: CUSTOM:bigExplosion

  unitsSpawnedOnDeath:                                  string(s)                     Spawns these units when dies. Comma separated unit identifiers.
    EXAMPLE: unitsSpawnedOnDeath: tank*5, hoverTank

  unitsSpawnedOnDeath_setToTeamOfLastAttacker:          bool                          Units spawned on death will appear on the last attacker team if true.
    EXAMPLE: unitsSpawnedOnDeath_setToTeamOfLastAttacker: true

  hideScorchMark:                                       bool                          Disables the death scorch mark leaved when unit dies if true.
    EXAMPLE: hideScorchMark: true

  soundOnDeath:                                         string(s)                     Sets a custom sound for this unit death.
    EXAMPLE: soundOnDeath: tankExplosion1.ogg, tankExplosion2.ogg

  effectOnDeathIfUnbuilt:                               effect(s) ref        1.14     If the unit was not completed, and is destroyed, play this effect. Defaults to effectOnDeath
    EXAMPLE: effectOnDeathIfUnbuilt: CUSTOM:implode

  autoTriggerCooldownTime:                              time (seconds)       1.13.3   Post automatic action cooldown (Not detection cooldown). Defaults to 1s. Warning: Setting this too low for many units might effect performance depending on the action effects.
    EXAMPLE: autoTriggerCooldownTime: 0.05s

  autoTriggerCooldownTime_allowDangerousHighCPU:        bool                          Allows for auto action cooldown lower than 0.2s. Default to false. Not recommended.
    EXAMPLE: autoTriggerCooldownTime_allowDangerousHighCPU: true

  autoTriggerCheckRate:                                 enum                 1.15     options: everyFrame (default), every4Frames, every8Frames. Note: all triggers regardless of check rate are checked when first created and after an auto trigger cooldown. Note: Adding [core]autoTriggerCheckRate:every8Frames to all-units.template could have a large performance boost for mods with complex autoTriggers.
    EXAMPLE: autoTriggerCheckRate:every8Frames

  autoTriggerCheckWhileNotBuilt:                        bool                 1.15     Defaults to false. autoTrigger of unit actions check even when not completely built if true
    EXAMPLE: autoTriggerCheckWhileNotBuilt: true

  updateUnitMemory:                                     memory               1.15p10  Faster memory update than [action_#] setUnitMemory. Useful on many applications.
    EXAMPLE: updateUnitMemory: timeCount = (1 + memory.timeCount)

  updateUnitMemoryRate:                                 int                           Sets how often the memory is updated. Defaults at 1s. settings it to 0 will update memory every frame.
    EXAMPLE: updateUnitMemoryRate: 0

  @memory                                                                    1.15     A template-friendly method of defineUnitMemory. Declare name followed by type, separated by a colon
    EXAMPLE: @memory fish:unit

  action_#_convertTo:                                   string               depr. 1.13 Deprecated in 1.13, use [action_x] sections instead
    EXAMPLE: action_1_convertTo: customTank_2

  action_#_pos:                                         float                         Order action appears in UI
    EXAMPLE: action_1_pos: 0.1

  action_#_price:                                       int                           The price of your action for the unit. (All your sub actions will be linked to the # you use)
    EXAMPLE: action_1_price: 1000

  action_#_text:                                        string                        A display text when you select your unit's action, used to explain it's purpose.
    EXAMPLE: action_1_text: Upgrade to Custom Tank 2

  action_#_description:                                 string                        The action description.
    EXAMPLE: action_1_description: -Converts the tank

  action_#_addEnergy:                                   float                         Adds energy to unit. Has no effect unless energyMax is set
    EXAMPLE: action_1_addEnergy: 10

  action_#_whenBuilding_cannotMove:                     bool                          Stops unit moving while action is being applied. Useful for deploy like actions.
    EXAMPLE: action_1_whenBuilding_cannotMove: true

  canBuild_#_name/pos/isLocked:                         string                        Use canBuild section instead.
    EXAMPLE: canBuild_1_name: tank


====================================================================================================
SECTION: [canBuild_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  name:                                                 string(s)            1.13.3   List of unit identifiers this unit can create. Can be buildings or units. Add "setRally" to create a rally button
    EXAMPLE: name: setRally, tank, hoverTank, heavyTank

  pos:                                                  float                         Order build link appears in this unit UI.
    EXAMPLE: pos: 0.1

  tech:                                                 int                           Tech level. Mostly just affects build link colour in this unit UI. Defaults to 1.
    EXAMPLE: tech: 2

  forceNano:                                            bool                          Builds target as if it was a building if true. (even if it's a unit)
    EXAMPLE: forceNano: true

  isVisible:                                            LogicBoolean                  Hide this build link if true in this unit UI.
    EXAMPLE: isVisible: if not self.energy(greaterThan=100)

  isLocked:                                             LogicBoolean                  Dynamically locks this build option and shows isLockedMessage if true.
    EXAMPLE: isLocked: if self.hp(lessThan=100)

  isLockedMessage:                                      string                        Set to tell to players why a unit is locked.
    EXAMPLE: isLockedMessage: -Needs 2 Barracks

  isLockedMessage_{LANG}:                               string                        LANG = ISO 639-1 Code to show this text instead when game is in this language.
    EXAMPLE: isLockedMessage_es: -Necesita 2 Cuarteles

  isLockedAlt:                                          LogicBoolean                  Another reason for this to be locked. Just allows a different message to be shown.
    EXAMPLE: isLockedAlt: if self.energy(greaterThan=90)

  isLockedAltMessage:                                   string                        Message for isLockedAlt.
    EXAMPLE: isLockedAltMessage: -Needs less energy

  isLockedAlt2:                                         LogicBoolean                  Like isLockedAlt but to show one more message.
    EXAMPLE: isLockedAlt2: if self.isMoving()

  isLockedAlt2Message:                                  string                        Message for isLockedAlt2.
    EXAMPLE: isLockedAlt2Message: -Needs to be quiet

  addResources:                                         price(s)                      Adds these resources to self when placing the building or producing the unit.
    EXAMPLE: addResources: ammo=5, setFlag=1

  price:                                                price(s)                      Overrides builded units/buildings price. Defaults to target unit prices.
    EXAMPLE: price: credits=1000, ammo=5

  isGuiBlinking:                                        LogicBoolean                  Generates a blinking effect in UI if true.
    EXAMPLE: isGuiBlinking: true


====================================================================================================
SECTION: [graphics]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  image:                                                file (image)                  File path to png image.
    EXAMPLE: image: body.png

  teamColoringMode:                                                          1.13     How pixels are used for team coloring, options: pureGreen, hueAdd, hueShift, disabled
    EXAMPLE: (Defaults to pureGreen if no code) teamColoringMode: disabled

  image_back:                                           file (image)                  An optional image drawn behind other units. Useful for factories that units exit
    EXAMPLE: image_back: floor.png

  image_shield                                          file (image)         1.14     Image to show as a custom shield
    EXAMPLE: image_shield: forcefield.png

  image_wreak:                                          file (image)                  Image to use when unit dies. Can be NONE to leave no wreak
    EXAMPLE: image_wreak: ded.png

  image_offsetX:                                        int                           Use this to adjust the graphics of a unit if it is too far off one side
    EXAMPLE: image_offsetX: 21

  image_offsetY:                                        int                           Use this to adjust the graphics of a unit if it is too far off one side
    EXAMPLE: image_offsetY: 41

  image_offsetH:                                        int                  1.15p9   Use this to adjust the height of the graphic, especially when using decals
    EXAMPLE: image_offsetH: 25

  image_floatingPointSize                               int                           Fixes of by 1 pixel sizing for images with widths and/or height that has odd value
    EXAMPLE: image_floatingPointSize: 10

  isVisible                                             logic                1.13.3   If false will hide the unit.
    EXAMPLE: isVisible: false

  isVisibleToEnemies                                    bool                 1.14     Only visible to player and allies when false. Recommend with showOnMinimapToEnemies. Useful for stealth units.
    EXAMPLE: isVisibleToEnemies: false

  scaleImagesTo:                                        float                         Resize image to fit this value in pixels.  Effects leg, and shadow images as well.
    EXAMPLE: scaleImagesTo: 1

  imageScale:                                           float                         Resize image. Defaults to 1. Effects leg, and shadow images as well.
    EXAMPLE: imageScale: 1

  drawLayer:                                            string                        Land units normally default to ground or ground2 if transport. Options: wreaks, underwater, bottom, ground, ground2, experimentals, air, top
    EXAMPLE: drawLayer: bottom

  whenBeingBuiltMakeTransparentTill                     float                1.13     How long the transparent effect is applied to incomplete units, set to 0 to disable completely. Default: 1
    EXAMPLE: whenBeingBuiltMakeTransparentTill: 0.4

  icon_zoomed_out                                       file (image)                  Sets the custom image of the unit icon on the zoomed out battle map
    EXAMPLE: icon_zoomed_out: unitIcon.png

  icon_zoomed_out_neverShow                             bool                          Sets wheather show the unit icon or not on the zoomed out battle map
    EXAMPLE: icon_zoomed_out_neverShow: false

  icon_build                                            file (image)                  Displays icon of the unit on Build Panel
    EXAMPLE: icon_build: iconFish.png

  image_turret:                                         file (image)                  Default image for all turrets, can also be set per turret
    EXAMPLE: image_turret: gun.png

  teamColorsOnTurret:                                   bool                          Defaults false. Apply team colours on turret as well. Also effects pre-turret images
    EXAMPLE: teamColorsOnTurret: true

  scaleTurretImagesTo:                                  float                         Will cause crash if image_turret is not specified, even if image is set per turret
    EXAMPLE: scaleTurretImagesTo: 1.2

  lock_body_rotation_with_main_turret:                  bool                          Locks body image locked to the direction of the turret named "1" (turret_1) or the first turret defined (if a turret_1 is not defined)
    EXAMPLE: lock_body_rotation_with_main_turret: true

  lock_leg_rotation_with_main_turret                    bool                 1.13     Locks legs and arms to the direction of the turret named "1" (turret_1) or the first turret defined (if a turret_1 is not defined)
    EXAMPLE: lock_leg_rotation_with_main_turret: true

  image_shadow:                                         file (image)                  Image file, NONE, AUTO or AUTO_ANIMATED. (AUTO  will use image and make it transparent black only.)
    EXAMPLE: image_shadow: shadow.png

  shadowOffsetX:                                        float                         Adjusts shadow horizontally
    EXAMPLE: shadowOffsetX: 10

  shadowOffsetY:                                        float                         Adjusts shadow vertically
    EXAMPLE: shadowOffsetY: 10

  image_shadow_frames                                   bool                 1.13.3   If shadow image should use frame animation of main image. .Alternative to AUTO_ANIMATED shadow value.
    EXAMPLE: image_shadow_frames: true

  lock_shadow_rotation_with_main_turret:                bool                          Locks body image shadow locked to first turret's direction
    EXAMPLE: lock_shadow_rotation_with_main_turret: true

  total_frames                                          int                           Defaults to 1. Animations require this.
    EXAMPLE: total_frames: 3

  frame_width:                                          int                           Calculated for you if total frames is set, but can be overridden
    EXAMPLE: frame_width: 40

  frame_height:                                         int                           Defaults to image height
    EXAMPLE: frame_height: 60

  default_frame                                         int                  1.14?    Default frame when not playing an animation. First frame is 0
    EXAMPLE: default_frame: 0

  splastEffect:                                         bool                          True to create a water wave effect when over water. Default false
    EXAMPLE: splastEffect: true

  dustEffect:                                           bool                          True to create a dust effect when over land. Default false
    EXAMPLE: dustEffect: true

  splastEffectReverse:                                  bool                          True to also create effect when unit is reversing
    EXAMPLE: splastEffectReverse: true

  dustEffectReverse:                                    bool                          True to also create effect when unit is reversing
    EXAMPLE: dustEffectReverse: true

  movementEffect                                        effect               1.13     Custom movement effect, can be anything
    EXAMPLE: movementEffect: smoke, CUSTOM:fastDust*2, CUSTOM:pop*5

  movementEffectReverse                                 effect               1.13     Reversed version of movementEffect: key
    EXAMPLE: movementEffectReverse: smoke, CUSTOM:fastDust*2, CUSTOM:pop*5

  movementEffectRate                                    float                1.13     Sets the frequency of effects being spawned while moving
    EXAMPLE: movementEffectRate: 10

  movementEffectReverseFlipEffects                      bool                 1.13     Create effect as if unit has rotated 180 when reversing
    EXAMPLE: movementEffectReverseFlipEffects: true

  repairEffect                                          effect ref                    Custom repair effect, can be anything. Replaces default effect from builders
    EXAMPLE: repairEffect: CUSTOM:mist*5

  repairEffectAtTarget                                  effect                        Custom repair effect towards the repaired target
    EXAMPLE: repairEffectAtTarget: CUSTOM:greensparks*2

  repairEffectRate                                      int                           Sets the frequency of effects being spawned while repairing. Defaults to 5, affects both sides.
    EXAMPLE: repairEffectRate: 20

  reclaimEffect                                         effect               1.13.3   Custom reclaim effect, can be anything. Replaces default effect from builders
    EXAMPLE: reclaimEffect: CUSTOM:sap*5

  reclaimEffectAtTarget                                 effect               1.13.3   Custom reclaim effect towards the reclaimed target
    EXAMPLE: reclaimEffectAtTarget: CUSTOM:redsparks*2

  reclaimEffectRate                                     int                  1.13.3   Sets the frequency of effects being spawned while reclaiming. Defaults to 5, affects both sides.
    EXAMPLE: reclaimEffectRate: 20

  rotate_with_direction:                                bool                          Defaults to true. Makes unit body image locked to 0 degrees when false. Often used with animation_direction_*
    EXAMPLE: rotate_with_direction: false

  animation_direction_units:                            float                         45 for 8 directions, 90 for 4 direction animation. Used with rotate_with_direction:false
    EXAMPLE: animation_direction_units: 45

  animation_direction_strideX:                          int                           Animation frames to offset on direction change.
    EXAMPLE: animation_direction_strideX: 40

  animation_direction_strideY:                          int                           Animation frames to offset on direction change. Used with frame_height.
    EXAMPLE: animation_direction_strideY: 60

  animation_direction_starting:                         float                         Direction for first frame
    EXAMPLE: animation_direction_starting: 90

  disableLowHpFire                                      bool                 1.13.3   Defaults to false. When true, removes the flames on severely damaged unit/structure.
    EXAMPLE: disableLowHpFire: false

  disableLowHpSmoke                                     bool                 1.13.3   Defaults to false. When true, removes the smokes on severely damaged unit/structure.
    EXAMPLE: disableLowHpSmoke: false

  showTransportBar:                                     bool                 1.13.3   Defaults to true. Shows the transport bar for transport units.
    EXAMPLE: showTransportBar: false

  showHealthBar                                         bool                 1.13.3   Defaults to true. Shows the health bar of a unit. (Still hidden even if true when "Always show unit health" setting is disabled.)
    EXAMPLE: showHealthBar: false

  showEnergyBar                                         bool                 1.13.3   Defaults to true. Shows the energy bar of a unit.
    EXAMPLE: showEnergyBar: false

  showShieldBar                                         bool                 1.14     Defaults to true. Shows the shield bar of a unit. Also affects the presence of set custom shield sprite.
    EXAMPLE: showShieldBar: false

  showQueueBar                                          bool                 1.14     Defaults to true. Shows the bar for build or action queue of a unit.
    EXAMPLE: showQueueBar: false

  showShotDelayBar                                      bool                 1.15     Defaults to true. Shows the shot delay/warmup of a unit before firing.
    EXAMPLE: showShotDelayBar: false

  showSelectionIndicator                                bool                 1.15p9   Defaults to true. Shows select circle/box if true
    EXAMPLE: showSelectionIndicator: false

  teamColorsUseHue:                                     bool                          False: Green pixels on unit gets converted to team color. True: Whole unit is tinted the team colour. Defaults to false

  animation_TYPE_start:                                 int                  depr. 1.13 TYPE can be set to: attack, moving, idle. Use [animation] section instead for more control
    EXAMPLE: animation_moving_start: 0

  animation_TYPE_end:                                   int                  depr. 1.13 End frame, must be larger then start
    EXAMPLE: animation_moving_end: 3

  animation_TYPE_scale_start:                           float                depr. 1.13 Scale unit image. Defaults to 1. Useful for bio units or breathing effects.

  animation_TYPE_scale_end:                             float                depr. 1.13 Scale unit image. Defaults to 1. Useful for bio units or breathing effects.

  animation_TYPE_speed:                                 float                depr. 1.13 Delay for each frame of animation. Larger values cause slower animation

  animation_TYPE_pingPong:                              bool                 depr. 1.13 Play animation in reverse before repeating. Useful with scale_start/scale_end


====================================================================================================
SECTION: [attack]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  canAttack:                                            bool                          If set to false, can not attack any unit. Overrides other canAttack options altogether.
    EXAMPLE: canAttack: true

  canAttackFlyingUnits:                                 LogicBoolean                  can also be narrowed per turret. Note: not required if canAttack is false.
    EXAMPLE: canAttackFlyingUnits: false

  canAttackLandUnits:                                   LogicBoolean                  can also be narrowed per turret in [turret_NAME]
    EXAMPLE: canAttackLandUnits: true

  canAttackUnderwaterUnits:                             LogicBoolean                  can also be narrowed per turret in [turret_NAME]
    EXAMPLE: canAttackUnderwaterUnits: false

  maxAttackRange:                                       float                         (multiplied by globalScale)
    EXAMPLE: maxAttackRange: 300

  shootDelay:                                           float                         Global delay, can also use delay on each turret. Set in seconds or ticks
    EXAMPLE: shootDelay: 21, shootDelay: 5s

  canAttackNotTouchingWaterUnits:                       LogicBoolean                  Default true. If false unit can only attack units in contact with the water. Used for units with torpedos. (can also be set per turret)
    EXAMPLE: canAttackNotTouchingWaterUnits: false

  canOnlyAttackUnitsWithTags                            tags                 1.13.3   Will only attack units that has the specified tags.
    EXAMPLE: canOnlyAttackUnitsWithTags: metallic, ceramic

  canOnlyAttackUnitsWithoutTags                         tags                 1.13.3   Can only attack units without the specified tags.
    EXAMPLE: canOnlyAttackUnitsWithoutTags: corrosive, magnetic

  turretMultiTargeting                                  bool                 1.13     Allow each turrets to fire at a different target at the same time. Very useful if [turret]limitingAngle is used
    EXAMPLE: turretMultiTargeting: true

  isMelee:                                              bool                          Used with a low attack range (like maxAttackRange: 9) makes src and target radius get added to range, and effects AI.
    EXAMPLE: isMelee: true

  meleeEngangementDistance                              int                  1.13.3   Makes unit move to attack nearby units. Defaults to 250 for melee, and 0 for non melee (Works even if non-melee, but might be unexpected to players)
    EXAMPLE: meleeEngangementDistance: 400

  turretRotateWithBody                                  bool                          Are all turrets rotated when body rotates. Defaults to true
    EXAMPLE: turretRotateWithBody: true

  attackMovement:                                       string                        normal/bomber. bomber attack movement will retreat when energy runs out
    EXAMPLE: attackMovement: bomber

  dieOnAttack:                                          bool                          Will die when it attacks.
    EXAMPLE: dieOnAttack: true

  isFixedFiring:                                        bool                          Must aim body at target to shoot. Will often make the unit need to stop before it can aim and shoot.
    EXAMPLE: isFixedFiring: true

  aimOffsetSpread:                                      float                         Offset each shot multiplied by target radius. Defaults to 0.6. aimOffsetSpread:0 will make unit always attack center
    EXAMPLE: aimOffsetSpread:0

  stopTargetingAfterFiring                              bool                 1.13     Unit stops targeting after firing a shot. Rarely used or needed.
    EXAMPLE: stopTargetingAfterFiring: true

  disablePassiveTargeting:                              bool                          Unit only attacks manually ordered target. Rarely used or needed.
    EXAMPLE: disablePassiveTargeting: true

  showRangeUIGuide                                      bool                 1.13.3   Will it show the range indicator. Useful for showing ranges in radar and related structures.
    EXAMPLE: showRangeUIGuide: true

  shootDelayMultiplier                                  float                1.15     Defaults to 1. Can be dynamically changed with setUnitStats
    EXAMPLE: shootDelayMultiplier: 0.75

  shootDamageMultiplier                                 float                1.15     Defaults to 1. Can be dynamically changed with setUnitStats
    EXAMPLE: shootDamageMultiplier: 2.43

  turretSize:                                           float                depr. 1.13 (multiplied by globalScale)

  turretTurnSpeed:                                      float                depr. 1.13 


====================================================================================================
SECTION: [turret_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  x:                                                    float                         Sets the horizontal position of the turret. 0 is center, negatives toward left, positives toward right.
    EXAMPLE: x: 0

  y:                                                    float                         Sets the vertical position of the turret.  0 is center, negatives toward bottom, positives toward top.
    EXAMPLE: y:  40

  copyFrom:                                             turret ref                    Copy all values from another turret as defaut values for this turret
    EXAMPLE: copyFrom: 1

  projectile                                            projectile ref                Projectile fired from this turret.
    EXAMPLE: projectile: torpedo

  altProjectile                                         projectile ref                Alternative projectile fired from this turret when altProjectileCondition is true
    EXAMPLE: altProjectile: spear

  altProjectileCondition                                LogicBoolean                  Used with altProjectile
    EXAMPLE: altProjectileCondition: if self.hp() < 100

  barrelX                                               float                1.13     Defaults to 0. Controls horizontal position for projectile spawn.
    EXAMPLE: barrelX: 0

  barrelY                                               float                1.13     Defaults to size. Note: size and barrelY have the same meaning
    EXAMPLE: barrelY: 5

  barrelHeight                                          float                1.13.3   Height of barrel in 3d. Effect projectile and shoot flame starting height
    EXAMPLE: barrelHeight: 10

  height:                                               float                1.15p9   Height of the unit in 3D, to be used with decals
    EXAMPLE: height: 30

  size:                                                 float                         Controls the distance between the center of the turret and the point from where projectiles spawn.
    EXAMPLE: size: 5

  turnSpeed                                             float                         Max turn speed of the turret
    EXAMPLE: turnSpeed: 2

  turnSpeedAcceleration                                 float                         Defaults to disabled, and full turn speed is used.
    EXAMPLE: turnSpeedAcceleration: 0.4

  turnSpeedDeceleration                                 float                         Defaults to turnSpeedAcceleration. Setting this higher than turn acceleration might allow faster targets to be hit
    EXAMPLE: turnSpeedDeceleration: 0.2

  idleDir                                               float                         Defaults to 0
    EXAMPLE: idleDir: 180

  idleDirReversing                                      float                1.13     Defaults to idleDir+180 unless attached to another turret (as attached turret will often be rotating when reversing)
    EXAMPLE: idleDirReversing: 30

  shouldResetTurret:                                    bool                          Defaults true. False to disable the reseting turret angle when idle
    EXAMPLE: shouldResetTurret: false

  idleSpin:                                             float                         Spin rate when idle, used on missile turrets
    EXAMPLE: idleSpin: 2

  idleSweepAngle                                        int                  1.14     Disabled by default. Controls how far the turret will "look" left and right
    EXAMPLE: idleSweepAngle: 35

  idleSweepDelay                                        float                1.14     Controls the delay between idleSweep movements
    EXAMPLE: idleSweepDelay: 20

  idleSweepSpeed                                        float                1.14     Controls the speed with which the turret sweeps when idle
    EXAMPLE: idleSweepSpeed: 0.3

  idleSweepCondition                                    LogicBoolean         1.14     Disable idle sweep if false
    EXAMPLE: idleSweepCondition: if self.hp() > 400

  idleSweepAddRandomDelay                               float                1.14     Default 1-20 depends on idleSweepDelay, used to stop sweep syncing up with other units
    EXAMPLE: idleSweepAddRandomDelay: 30

  idleSweepAddRandomAngle                               int                  1.14     Default 0
    EXAMPLE: idleSweepAddRandomAngle: 15

  attachedTo:                                           turret ref                    A specific turret to connect to, it will rotate with it. No correlation to [attachment_x]
    EXAMPLE: attachedTo: base

  slave:                                                bool                          Locks this turret's direction and shot cooldown to attached turret. Often used with warmup for multiple barrel guns
    EXAMPLE: slave: true

  isMainNanoTurret:                                     bool                          Defaults to false. Turret to use for creating buildings, etc. should only be true on one turret, and should have canShoot set to false
    EXAMPLE: isMainNanoTurret: true

  energyUsage:                                          float                         Required energy to fire weapon. Same as resourceUsage: energy=X
    EXAMPLE: energyUsage: 1

  resourceUsage                                         price                         can be in credits/energy/hp/shield/ammo. Stops firing if not met
    EXAMPLE: resourceUsage: credits=5, energy=5, hp=100, shield=5, ammo=1

  delay:                                                float                         Override global shootDelay for this turret
    EXAMPLE: delay: 2s

  linkDelayWithTurret                                   turret ref                    When this other turret fires the cooldown delay on this turret will be reset/removed
    EXAMPLE: linkDelayWithTurret: 1

  warmup:                                               float                         Delay before firing a shot.
    EXAMPLE: warmup: 5s

  warmupCallDownRate                                    float                         Rate to reduce warmup when turret is not ready to fire at any targets
    EXAMPLE: warmupCallDownRate: 10

  warmupNoReset                                         bool                          Defaults to false. When true warmup is not reset after firing a shot and turret doesn't wait for warnup. Used with warmupCallDownRate and warmupShootDelayTransfer.
    EXAMPLE: warmupNoReset: true

  warmupShootDelayTransfer                              float                         Defaults to 0, amount to reduces the next shot delay depending on warmup. When used with warmupNoReset, can make a each shot faster.
    EXAMPLE: warmupShootDelayTransfer: 20

  onShoot_freezeBodyMovementFor:                        float                1.13.3   Freezes body movement while shooting.
    EXAMPLE: onShoot_freezeBodyMovementFor: 2s

  barrelOffsetX_onOddShots:                             float                1.14     0 by default. Sets a barrelX offset only during odd numbered shots, useful for twin-barreled units. Use with barrelX. Use the opposite value of barrelX for most cases.
    EXAMPLE: barrelOffsetX_onOddShots: -10

  aimOffsetSpread:                                                           1.14     Sets the shot inaccuracy. 0 will make all shots land on target's center
    EXAMPLE: aimOffsetSpread: 14

  canShoot:                                             bool                          Defaults to true
    EXAMPLE: canShoot: true

  canAttackFlyingUnits:                                 LogicBoolean                  Narrows targeting for this turret, note targeting for the whole unit in [attack] is applied first. (so you can only use this to target less not more)
    EXAMPLE: canAttackFlyingUnits: false

  canAttackLandUnits:                                   LogicBoolean                  Default true. If false unit cannot attack surface units.
    EXAMPLE: canAttackLandUnits: true

  canAttackUnderwaterUnits:                             LogicBoolean                  Default true. If false unit cannot attack underwaterunits.
    EXAMPLE: canAttackUnderwaterUnits: true

  canAttackNotTouchingWaterUnits:                       LogicBoolean                  Default true. If false unit can only attack units in contact with the water. Used for units with torpedos.
    EXAMPLE: canAttackNotTouchingWaterUnits: false

  canOnlyAttackUnitsWithTags:                           tags                 1.13.3   Only attacks potential targets with specific tags
    EXAMPLE: canOnlyAttackUnitsWithTags: shelled, armoured

  canOnlyAttackUnitsWithoutTags:                        tags                 1.13.3   Only attacks potential targets without specific tags
    EXAMPLE: canOnlyAttackUnitsWithoutTags: corrosive, fleshy

  canAttackCondition:                                   LogicBoolean                  Normally, used to optionally disable a turret based on a LogicBoolean. Eg: this unit's height
    EXAMPLE: canAttackCondition: if not self.flying

  canAttackMaxAngle:                                    float                1.14     Max angle to target for turret to be allowed for fire. Defaults to 5, don't set lower. Can be set to 181 for turrets that don't need to turn to fire missiles.
    EXAMPLE: canAttackMaxAngle: 181

  clearTurretTargetAfterFiring:                         bool                 1.13     Clears the turrets sub-target when using multi-targeting
    EXAMPLE: clearTurretTargetAfterFiring:

  limitingRange:                                        float                         Make this turret have less range than the maxAttackRange. Do not apply this to all turrets change maxAttackRange instead.
    EXAMPLE: limitingRange: 100

  limitingAngle                                         int                  1.13     Linked with idleDir. Turret will only be able to fire at units +/- this angle.
    EXAMPLE: limitingAngle: 60

  limitingMinRange:                                     int                  1.13     Sets minimum range for turret.
    EXAMPLE: limitingMinRange: 200

  interceptProjectiles_withTags:                        tags                 1.13     Currently used with anti-nuke units.
    EXAMPLE: interceptProjectiles_withTags: nuke

  interceptProjectiles_andTargetingGroundUnderDistance: int                           Minimum distance required for intercepting projectile
    EXAMPLE: interceptProjectiles_andTargetingGroundUnderDistance: 100

  interceptProjectiles_andUnderDistance:                int                           Defaults to 2000, distance inflight before firing
    EXAMPLE: interceptProjectiles_andUnderDistance: 300

  interceptProjectiles_andOverHeight:                   int                           Defaults to 0. Sets the projectile target height limit
    EXAMPLE: interceptProjectiles_andOverHeight: 10

  laserDefenceEnergyUse:                                float                         Set to enable a projectile laser defence from this turret. Should also set the energyMax in core.
    EXAMPLE: laserDefenceEnergyUse: 0.25

  invisible:                                            LogicBoolean                  Don't render this turret, but still can shoot, etc.
    EXAMPLE: invisible: true

  image:                                                file (image)                  Use custom image. Overrides unit's main turret image
    EXAMPLE: image: gun.png

  image_applyTeamColors                                 bool                          When true, the specific turret applies team colors over the special greens in the sprite.
    EXAMPLE: image_applyTeamColors: true

  image_drawOffsetX                                     float                         Moves the image horizontally from the turret center.
    EXAMPLE: image_drawOffsetX: 0

  image_drawOffsetY                                     float                         Moves the image verticallly from the turret center.
    EXAMPLE: image_drawOffsetY: 30

  chargeEffectImage:                                    file (image)                  Used with warmup. Shows a scaling effect image on turret barrel when charging.
    EXAMPLE: chargeEffectImage: glow.png

  warmupStartEffect                                     effect ref                    Spawns specified effects when the specific turret is charging/warming up
    EXAMPLE: warmupStartEffect: CUSTOM:abosrb

  shoot_sound:                                          string                        Can be linked to an .ogg or .wav file, or one of the default game sounds (list at bottom of reference)
    EXAMPLE: shoot_sound: tank_firing | shoot_sound: missile.wav | shoot_sound: ROOT:audio/shoot.ogg

  shoot_sound_vol:                                      float                         Sets the volume of the shoot sound
    EXAMPLE: shoot_sound_vol: 0.4

  shoot_flame:                                          effects                       Current types are: small, large, smoke, shockwave, or CUSTOM: effectSectionName
    EXAMPLE: eg: shoot_flame: smoke, CUSTOM:lightFade, CUSTOM:pop*5

  shoot_light                                           color                         Produces a colored light upon shooting
    EXAMPLE: shoot_light: #fafafa

  onShoot_playAnimation                                 animation ref        1.13     Play a custom animation from an [animation] section after firing this turret
    EXAMPLE: onShoot_playAnimation: heatUp

  onShoot_triggerActions                                action refs          1.14     Trigger these actions each time this turret fires
    EXAMPLE: onShoot_triggerActions: spawnShells

  unloadUpToXUnitsAndGiveAttackOrder                    int                  1.15     Unloads X units at turret barrel locations and gives them the attack order of turret target
    EXAMPLE: unloadUpToXUnitsAndGiveAttackOrder: 1

  recoilOffset                                          float                         Push turret forward or back after firing for a recoil effect. Value in pixels.
    EXAMPLE: recoilOffset: 10

  recoilOutTime                                         float                         Time to get to offset position after firing
    EXAMPLE: recoilOutTime: 1s

  recoilReturnTime                                      float                         Time to return to default position
    EXAMPLE: recoilReturnTime: 0.5s

  showRangeUIGuide                                      bool                 1.13.3   Defaults to true. Displays the attack range for this turret
    EXAMPLE: showRangeUIGuide: true


====================================================================================================
SECTION: [projectile_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  life:                                                                               How long till this projectile gets removed if it hasn't hit a target, 300 might be a good starting point, change depending on speed and range
    EXAMPLE: life: 300

  deflectionPower:                                      float                         Defaults to 1. Energy needed for laser defence to deflect. -1 to disable deflection (only disable for special weapons like flames)
    EXAMPLE: deflectionPower: -1

  explodeOnEndOfLife                                    bool                 1.13     Default to false. True to explode at end of life with all side effects and area damage instead of disappearing. Good for making area-denial weapons.
    EXAMPLE: explodeOnEndOfLife: true

  autoTargetingOnDeadTarget:                            bool                          Retarget to nearby unit if target dies while in transit
    EXAMPLE: autoTargetingOnDeadTarget: true

  autoTargetingOnDeadTargetRange                        int                  1.14     The range which it will select a new target if old target has died
    EXAMPLE: autoTargetingOnDeadTargetRange: 100

  autoTargetingOnDeadTargetLead                         float                1.14     The lead it will try to have on the new target
    EXAMPLE: autoTargetingOnDeadTargetLead: 300

  unloadUpToXUnitsFromSource                            int                  1.13     Unload X units from source unit, to projectile explode location
    EXAMPLE: unloadUpToXUnitsFromSource: 2

  teleportSource                                        bool                 1.13     Move unit that shot this projectile to projectile explode location
    EXAMPLE: teleportSource: true

  spawnUnit:                                            projectile ref       1.13     Spawn new units of this type at projectile explode location
    EXAMPLE: eg: spawnUnit: heavyTank, tank*5, hoverTank(offsetX=10)

  spawnProjectilesOnEndOfLife                           projectile ref       1.14     Spawns new projectiles on end of life, useful for secondary projectiles
    EXAMPLE: spawnProjectilesOnEndOfLife: torpedo_split(offsetDir=90), torpedo_split(offsetDir=-90)

  spawnProjectilesOnExplode                             projectile ref       1.14     Projectiles to spawns when this projectile hits it's target
    EXAMPLE: spawnProjectilesOnExplode: flachette*4

  spawnProjectilesOnCreate                                                   1.14     Spawns projectiles on creation of this projectile, useful for making true shotgun-like projectile spread
    EXAMPLE: spawnProjectilesOnCreate: shotgun*12

  convertHitToSourceTeam                                bool                 1.13.3   Convert units hit to the team that fired this projectile. Useful to make capturing systems
    EXAMPLE: convertHitToSourceTeam: true

  tags                                                  tags                 1.13     Used for projectile interceptions  (e.g. Nuke and Anti-Nuke Interaction) and in [action] autoTriggerOnEvent: tookDamage(withTag="[tagName]")
    EXAMPLE: tags: nuke, missile

  flameWeapon:                                          bool                          Generates small flames on hit (only cosmetic)
    EXAMPLE: flameWeapon: true

  delayedStartTimer                                     float                1.13     Hide for x time before showing and updating effect.
    EXAMPLE: delayedStartTimer: 2s

  directDamage:                                         int / 1.16: dynamic  1.0 / updated 1.16 Damage to target unit on hit. Does not work with targetGround:true as it won't have a clear unit to target
    EXAMPLE: directDamage: 30

  areaDamage:                                           int / 1.16: dynamic  1.0 / updated 1.16 Damages on arrival of target with an area effect, use areaRadius to adjust size of damage. targetGround needs this to damage
    EXAMPLE: areaDamage: 10

  areaRadius:                                           float                1.13     How wide areaDamage effects. Note this drops off (unless areaDamageNoFalloff is used)
    EXAMPLE: areaRadius: 100

  areaDamageNoFalloff                                   bool                          Removes the falloff from areaDamage
    EXAMPLE: areaDamageNoFalloff: true

  areaRadiusFromEdge                                    bool                          Applies damage from edge of units instead of center. Mostly effects large units.
    EXAMPLE: areaRadiusFromEdge: true

  areaExpandTime                                        float                1.13     Applies area damage as an expanding blast wave rather than instantly. Useful for nuke projectiles
    EXAMPLE: areaExpandTime: 1s

  areaHitAirAndLandAtSameTime                           bool                 1.13     Defaults to false
    EXAMPLE: areaHitAirAndLandAtSameTime: true

  areaHitUnderwaterAlways                               bool                          Defaults to false
    EXAMPLE: areaHitUnderwaterAlways: true

  areaIgnoreUnitsCloserThan                             int                           Units closer than this range aren't effected. Rarely needed. Not recommended for normal projectiles.
    EXAMPLE: areaIgnoreUnitsCloserThan: 10

  buildingDamageMultiplier                              float                1.13     Defaults to 1
    EXAMPLE: buildingDamageMultiplier: 3

  shieldDamageMultiplier                                float                1.13     Defaults to 1.  0 to do no damage to shields and 2 to do double damage
    EXAMPLE: shieldDamageMultiplier: 1.4

  shieldDefectionMultiplier                             float                1.13     Defaults to 1. The amount of shield to bypass. 0 to ignore shields and directly damage hull
    EXAMPLE: shieldDefectionMultiplier: 1.55

  hullDamageMultiplier                                  float                1.14     Defaults to 1. Can be used to create EMP weapons that affect shields only. 0 to ignore hull and only damage shields
    EXAMPLE: hullDamageMultiplier: 2

  ignoreParentShootDamageMultiplier                     bool                 1.15     Sets whether multipliers are applied or not regardless of multipliers set in attack or setUnitStats
    EXAMPLE: ignoreParentShootDamageMultiplier: true

  armourIgnoreAmount                                    int                           Amount of armour to ignore on target and do damage as if this armour was not there
    EXAMPLE: armourIgnoreAmount

  friendlyFire                                          bool/string          1.13     Lets area effect projectiles damage own team units (can't damage allies). Useful for nuke-like weapons
    EXAMPLE: friendlyFire: false / friendlyFire: true / friendlyFire: only-ignoreEnemy

  mutatorX_ifUnitWithTags                               tags                 1.13.3   Applies mutators to this projectile if target has corresponding tags
    EXAMPLE: mutator1_ifUnitWithTags: infantry

  mutatorX_ifUnitWithoutTags                            tags                 1.13.3   Same as ifUnitWithTags, but applies if target doesn't have the set tags
    EXAMPLE: mutator1_ifUnitWithoutTags: strongArmour

  mutatorX_directDamageMultiplier                       float                1.13.3   Changes directDamage. Defaults to 1. Be careful not to confuse players using this as the effect may not be clear. Use amour instead when possible. Replace X with desired name
    EXAMPLE: mutatorX_directDamageMultiplier: 5

  mutatorX_areaDamageMultiplier                         float                1.13.3   Same as directDamageMultiplier but for areaDamage. Defaults to 1.
    EXAMPLE: mutatorX_areaDamageMultiplier: 1.2

  mutatorX_changedExplodeEffect                         effect               1.13.3   Change explode effect if this mutator is active. Eg make a bounce off amour effect. Helps to make the damage change more clear to players (Doesn't work with targetGround.)
    EXAMPLE: mutatorX_changedExplodeEffect: CUSTOM:specialBlast

  mutatorX_addResourcesDirectHit                        resource             1.14     Add resource to all direct hit units. Warning: Be careful not be break units from other mods by adding random resources or energy to them that they don't expect.
    EXAMPLE: mutatorX_addResourcesDirectHit: oil=1

  mutatorX_addResourcesAreaHit                          resource             1.14     Add resource to all area hit units. Warning: Be careful not be break units from other mods by adding random resources or energy to them that they don't expect.
    EXAMPLE: mutatorX_addResourcesAreaHit: rust=4

  targetGround                                          bool                          Target ground, and don't home in on target. Note: only areaDamage is applied if targeting ground.
    EXAMPLE: targetGround: true

  targetGround_includeTargetHeight                      bool                 1.14     Default false. for area affect AA weapons
    EXAMPLE: targetGround_includeTargetHeight: 40

  targetGroundHeightOffset                              float                1.14     Default 0. for shooting over or under a target. Useful for projectiles that split and rain down.
    EXAMPLE: targetGroundHeightOffset: 10

  speed:                                                float                         Projectile default travel speed
    EXAMPLE: speed: 3

  targetSpeed:                                          float                         Accelerate to this speed
    EXAMPLE: targetSpeed: 5

  targetSpeedAcceleration                               float                1.13     Controls the speed rampup for targetSpeed
    EXAMPLE: targetSpeedAcceleration: 0.3

  ballistic:                                            bool                          Makes projectiles fly up into the air and come down, instead of going in a straight line
    EXAMPLE: ballistic: true

  ballistic_delaymove_height:                           float                         Sets up to how high the projectile needs to be before moving normally
    EXAMPLE: ballistic_delaymove_height: 20

  ballistic_height:                                     float                         Sets the target height of the projectile
    EXAMPLE: ballistic_height: 10

  targetGroundSpread:                                   float                         Randomly makes the shot inaccurate by this amount. Also used by weapons like the flamethrower
    EXAMPLE: targetGroundSpread: 20

  speedSpread:                                          float                         Randomly change the starting projectile speed by this amount
    EXAMPLE: speedSpread: 3

  instant                                               bool                          Hit target instantly
    EXAMPLE: instant: true

  instantReuseLast:                                     bool                          Recycles last projectile fired, only one projectile ever exists. Can turn lasers into beam weapons by using lower rate of fire and setting this to true
    EXAMPLE: instantReuseLast: true

  instantReuseLast_alsoChangeTurretAim                  bool                 1.14     Make turret's aim include last projectile's spread and sweep offsets, useful for beam weapons
    EXAMPLE: instantReuseLast_alsoChangeTurretAim: true

  instantReuseLast_keepAreaDamageList                   bool                 1.14     Default false. Keeping the list was the normal behaviour in 1.13 making area damage not apply a second time but this is not useful. Use this only if you want the old behaviour.
    EXAMPLE: instantReuseLast_keepAreaDamageList: true

  interceptProjectile_removeTargetLifeOnly              bool                 1.14     Defaults to false. When false projectiles are just removed. Could be true to make hit projectiles explode or split when hit
    EXAMPLE: interceptProjectile_removeTargetLifeOnly: true

  disableLeadTargeting                                  bool                 1.13     Disable the lead targeting calculations when aiming at a moving target. Defaults false.
    EXAMPLE: disableLeadTargeting: true

  leadTargetingSpeedCalculation                         float                1.13     The expected speed of this projectile for targetGround lead target calculation. Defaults to 'targetSpeed' if set otherwise 'speed'.
    EXAMPLE: leadTargetingSpeedCalculation: 2

  initialUnguidedSpeedHeight                                                 1.13.3   Sets vertical speed for projectiles with targetGround. Use gravity to make smooth arching projectiles. Better have gravity value slightly slower than this key to produce the arches.
    EXAMPLE: initialUnguidedSpeedHeight: 30

  gravity:                                                                   1.13.3   Controls the pull for projectiles that target ground. Use together with initialUnguidedSpeedHeight
    EXAMPLE: gravity: 29

  turnSpeed                                             float                1.14     Limits the turn speed of a projectile, making them inaccurate even with directDamage. Zero value will make it act like if targetGround is set true, but air target friendly option.
    EXAMPLE: turnSpeed: 0

  wobbleAmplitude                                       float                1.14     How wide the projectile will wobble
    EXAMPLE: wobbleAmplitude: 10

  wobbleFrequency                                       float                1.14     How often the projectile will wobble
    EXAMPLE: wobbleFrequency: 4

  pushForce                                             float                1.14     Push (or pull with a negative value) the units that get hit. Divided by target mass
    EXAMPLE: pushForce: 2

  pushVelocity                                          float                1.14     Push (or pull with a negative value) the units that get hit. Ignores target mass
    EXAMPLE: pushVelocity: 5

  moveWithParent                                        bool                 1.14     Move projectile as parent moves. Useful for beam effects that need to stick to source turret.
    EXAMPLE: moveWithParent: true

  sweepOffset                                           float                1.14     Useful for beam effects.
    EXAMPLE: sweepOffset: 0.5

  sweepOffsetFromTargetRadius                           float                1.14     Add to sweep offset by factor of target's radius. 0.4 would be 40%
    EXAMPLE: sweepOffsetFromTargetRadius: 0.4

  sweepSpeed                                            float                1.14     Useful for beam effects.
    EXAMPLE: sweepSpeed: 1.5

  retargetingInFlight                                   bool                 1.14     Can retarget a new target mid-flight, perfect for flak-style weapons and projectiles that collide
    EXAMPLE: retargetingInFlight: true

  retargetingInFlightSearchDelay                        float/time           1.14     How long between searching for new targets. Default 5
    EXAMPLE: retargetingInFlightSearchDelay: 1s

  retargetingInFlightSearchRange                        int                  1.14     Range which targets are reselected. Default 120
    EXAMPLE: retargetingInFlightSearchRange: 300

  retargetingInFlightSearchLead                         float                1.14     The lead of the projectile to try to hit the target. Default 15
    EXAMPLE: retargetingInFlightSearchLead: 30

  retargetingInFlightSearchOnlyTags                     tag ref              1.14     Only retarget units with these tags
    EXAMPLE: retargetingInFlightSearchOnlyTags: lightArmor

  color                                                 color                         Recolors this projectile using a hex value.
    EXAMPLE: color: #bebe50

  invisible                                             bool                 1.13.3   When true, the projectile is not rendered but still functional.
    EXAMPLE: invisible: true

  image:                                                file (image)                  Use custom image. Overrides drawType and frame
    EXAMPLE: image: bullet

  drawType                                              int                           Built-in image to use. 0:projectiles.png 1:projectiles_large.png 2:projectiles2.png. Refer to end of sheet for the projectile images.
    EXAMPLE: drawType:1

  drawSize:                                             float                         Scale image. Defaults to 1
    EXAMPLE: drawSize: 1.5

  frame                                                 int                           Built-in image frame to use, starts at zero.
    EXAMPLE: frame: 0

  hitSound:                                             bool                          Default true
    EXAMPLE: hitSound: true

  explodeEffect                                         effect ref list      1.13     Produces the specified effects upon explosion.
    EXAMPLE: explodeEffect: smallExplosion, CUSTOM:myExplodeEffect

  explodeEffectOnShield                                 effect ref list      1.13     Use this effect if shield is active on target
    EXAMPLE: explodeEffectOnShield: CUSTOM:EMPwave

  teamColorRatio                                        float                1.13     Mix 0-1 of team colour into color field
    EXAMPLE: teamColorRatio: 1

  teamColorRatio_sourceRatio                            float                1.14     default is (1-teamColorRatio). Keep more of color when mixing. Note this might saturate colors.
    EXAMPLE: teamColorRatio_sourceRatio: 1

  drawUnderUnits                                        bool                 1.13     If true, renders the projectile under units. Great for torpedos.
    EXAMPLE: drawUnderUnits: true

  effectOnCreate                                        effect ref list      1.13     Produces specified effects upon creation of the projectile.
    EXAMPLE: effectOnCreate: CUSTOM:puff

  shouldRevealFog                                       bool                 1.13     Reveal fog to player on explode
    EXAMPLE: shouldRevealFog: true

  alwaysVisibleInFog                                    bool                 1.13     Renders the projectile even when fog is present
    EXAMPLE: alwaysVisibleInFog: false

  nukeWeapon                                            bool                 1.13     Shows on mini-map when fired. Some other side effects as well, like nuke explosion effect
    EXAMPLE: nukeWeapon: true

  trailEffect                                           bool/effect                   true for built-in defaults, but can also point to any custom effects
    EXAMPLE: trailEffect: true, trailEffect: CUSTOM:rocketThrust

  trailEffectRate                                       float                1.13     Defaults to 3
    EXAMPLE: trailEffectRate: 4

  lightCastOnGround                                     bool                          Renders light under the projectile
    EXAMPLE: lightCastOnGround: true

  lightSize:                                            float                         Sets the size of the light emitted by the projectile. 1 value = 1 tile
    EXAMPLE: lightSize: 1.5

  lightColor                                            color                         Sets the color of the light emitted by the projectile.
    EXAMPLE: lightColor: #ffe92b

  largeHitEffect:                                       bool                          Creates a large explosion and accompanying sound on hit (only cosmetic)
    EXAMPLE: largeHitEffect: true

  lightingEffect:                                       bool                          Draw as lighting works best with instant:true
    EXAMPLE: lightingEffect: true

  laserEffect:                                          bool                          Draw as laser works best with instant:true
    EXAMPLE: laserEffect: true

  beamImage                                             file (image)         1.14     Image to use for beam and laser effect type projectiles. Image is repeated vertically depending on the distance to target. Beam image should be 20 pixels or longer. Width does not matter, only the height.
    EXAMPLE: beamImage: beam.png

  beamImageOffsetRate                                   float                1.14     Sets how fast the beam image moves towards or away from the target
    EXAMPLE: beamImageOffsetRate: 1

  beamImageStart                                        file (image)         1.14     Sprite for the origin point of the custom beam
    EXAMPLE: beamImageStart: beamStart.png

  beamImageStartRotated                                 bool                 1.14     Defaults false. True to rotate with turret angle
    EXAMPLE: beamImageStartRotated: true

  beamImageEnd                                          file (image)         1.14     Sprite for the end point of the custom beam
    EXAMPLE: beamImageEnd: beamEnd.png

  beamImageEndRotated                                   bool                 1.14     Defaults false
    EXAMPLE: beamImageEndRotated: true


====================================================================================================
SECTION: [movement]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  movementType:                                         string                        Defines what kind of terrain the unit will be able to move, along with other properties. Can use only one: NONE, LAND, AIR, WATER, HOVER, BUILDING, OVER_CLIFF, OVER_CLIFF_WATER
    EXAMPLE: movementType: LAND

  slowDeathFall:                                        bool                          Used with large aircraft. Makes the unit fall slowly while maintaining its speed at the time of death.
    EXAMPLE: slowDeathFall: true

  moveSpeed:                                            float                         Maximum movement speed of the unit. A value of 1 means (1 pixel * 60) per second (or 3 tiles per second)
    EXAMPLE: moveSpeed: 1.2

  moveAccelerationSpeed:                                float                         Defines how fast units accelerate to max speed.
    EXAMPLE: moveAccelerationSpeed: 0.07

  moveDecelerationSpeed:                                float                         Don't make this too low or units will have trouble stopping at waypoints
    EXAMPLE: moveDecelerationSpeed: 0.17

  reverseSpeedPercentage:                               float                         0.6 default. Over 0.4 will reverse for short distances (at 40% speed). If set to 1 will drive in reverse same as forwards. Useful if slow turning
    EXAMPLE: reverseSpeedPercentage: 0

  landOnGround:                                         bool                          Should flying unit land when idle.
    EXAMPLE: landOnGround: false

  targetHeight:                                         float                         Defaults to 0 but if AIR movementType default is 35
    EXAMPLE: targetHeight: 25

  targetHeightDrift:                                    float                         Smooth animated height change. Defaults to 0 but if AIR movementType default is 1.5
    EXAMPLE: targetHeightDrift: 1

  startingHeightOffset:                                 float                         Sets the initial height on spawn. Defaults at 0.
    EXAMPLE: startingHeightOffset: 40

  heightChangeRate:                                     float                1.14     Rate at which the unit changes height, either from converting or drifting
    EXAMPLE: heightChangeRate: 3

  fallingAcceleration:                                  float                1.14     The acceleration in which a unit drops
    EXAMPLE: fallingAcceleration:

  fallingAccelerationDead:                              float                1.14     fallingAcceleration but when destroyed
    EXAMPLE: fallingAccelerationDead:

  maxTurnSpeed:                                         float                         Sets the top turning speed of a unit
    EXAMPLE: maxTurnSpeed: 4

  turnAcceleration:                                     float                         Defines how fast units accelerate to max turn speed.
    EXAMPLE: turnAcceleration: 1

  moveSlidingMode:                                      bool                          Makes the unit slide when moveDecelerationSpeed is lower, making them drift and feel natural
    EXAMPLE: moveSlidingMode: true

  moveIgnoringBody:                                     bool                          Allows the unit to move without fully turning in the direction its moving, useful for ships and air units
    EXAMPLE: moveIgnoringBody: true

  moveSlidingDir:                                       int                           Sets direction when sliding
    EXAMPLE: moveSlidingDir: 180

  joinsGroupFormations:                                 bool                          Defaults to true. Changing not recommended. When false, the unit will directly move to the assigned waypoint without taking space consideration from neighboring units.
    EXAMPLE: joinsGroupFormations: false


====================================================================================================
SECTION: [ai]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  useAsBuilder:                                         bool                          Set to true if unit can build or repair buildings. Defaults to [core]isBuilder.
    EXAMPLE: useAsBuilder:

  useAsTransport                                        bool                          Defaults to true if unit can transport units
    EXAMPLE: useAsTransport

  useAsHarvester                                        bool                          Defaults to true if unit can reclaim resources
    EXAMPLE: useAsHarvester

  disableUse:                                           bool                          Disallow AI building this unit or building
    EXAMPLE: disableUse:

  aiTags                                                tags                          SPECULATIVE - sets separate set of tags for AI use
    EXAMPLE: aiTags: nukeLauncher, launcher

  ai_upgradePriority                                    float                         Defaults to 0.06. Set between 0-1, higher means AI is more likely to upgrade this unit before others
    EXAMPLE: ai_upgradePriority

  maxGlobal:                                            int                           Maximum amount allowed for a specific structure for an AI team per map
    EXAMPLE: maxGlobal: 50

  maxEachBase:                                          int                           Maximum amount allowed for a specific structure for an AI team per "base", usually around an extractor or spawn point. To check for an AI base in sandbox, enable Debug Mode and Click Shift + F3
    EXAMPLE: maxEachBase: 10

  buildPriority:                                        float                         0-1. AI uses 0.8 for first land factory, 0.48 for air factory, 0.47 for first turret.
    EXAMPLE: buildPriority: 1

  noneInBaseExtraPriority:                              float                         Adds to buildPriority, if this unit doesn't exist in the AIs base
    EXAMPLE: noneInBaseExtraPriority: 2

  noneGlobalExtraPriority:                              float                         Adds to buildPriority, if this unit doesn't exist in the any where on the map
    EXAMPLE: noneGlobalExtraPriority: 4

  recommendedInEachBaseNum                              float                         Defaults to 0
    EXAMPLE: recommendedInEachBaseNum: 2

  recommendedInEachBasePriorityIfUnmet                  float                         Defaults to 0.5. Overrides buildPriority if recommended in base is too low.
    EXAMPLE: recommendedInEachBasePriorityIfUnmet: 4

  upgradedFrom:                                         string                        Create link to another unit to preserve max counts for upgraded and non-upgraded types in same base.
    EXAMPLE: upgradedFrom: builderLevel1

  notPassivelyTargetedByOtherUnits                      bool                 1.14     Useful for walls, etc
    EXAMPLE: notPassivelyTargetedByOtherUnits: true

  lowPriorityTargetForOtherUnits                        bool                 1.14     Useful for units that cannot attack back. Eg walls
    EXAMPLE: lowPriorityTargetForOtherUnits: true

  whenUsingAsHarvester_recommendedInEachBase            int                  1.13.3   Assigns unit of the same name with certain amount on an AI base
    EXAMPLE: whenUsingAsHarvester_recommendedInEachBase: 3

  whenUsingAsHarvester_recommendedGlobal                int                  1.13.3   Assigns unit of the same name with certain amount on map
    EXAMPLE: whenUsingAsHarvester_recommendedGlobal: 20

  whenUsingAsHarvester_includeOtherHarvesterCounts      bool                 1.13.3   Includes all harvester types on a single counter
    EXAMPLE: whenUsingAsHarvester_includeOtherHarvesterCounts: true

  onlyUseAsHarvester_ifBaseHasUnitTagged                string               1.13.3   Only sets the harvester unit as a harvester if an AI base has a unit with particular tag(s)
    EXAMPLE: onlyUseAsHarvester_ifBaseHasUnitTagged: mineral, wood, stones


====================================================================================================
SECTION: [leg_#] / [arm_#]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  x:                                                    float                         Sets position of the foot on the X axis.
    EXAMPLE: x: 10

  y:                                                    float                         Sets position of the foot on the Y axis.
    EXAMPLE: y: 20

  copyFrom:                                             int                           Copy from another leg. Useful to only need to set leg values once
    EXAMPLE: copyFrom: 1

  attach_x:                                             float                         Sets the leg's attach point on the X axis.
    EXAMPLE: attach_x: 10

  attach_y:                                             float                         Sets the leg's attach point on the Y axis.
    EXAMPLE: attach_y: 0

  rotateSpeed:                                          float                         Sets the leg's rotation speed on movement
    EXAMPLE: rotateSpeed: 2

  endDirOffset                                          int                           Target foot/end rotation relative to body
    EXAMPLE: endDirOffset: 45

  lockMovement                                          bool                          Lock to unit body. Useful if walking unit converted to a flying unit.
    EXAMPLE: lockMovement: true

  heightSpeed:                                          float                         Sets how fast the leg rises while walking
    EXAMPLE: heightSpeed: 2

  moveSpeed                                             float                         Sets how fast the leg moves while walking
    EXAMPLE: moveSpeed: 3

  moveWarmUp                                                                          Delay before the leg moved
    EXAMPLE: moveWarmUp: 2

  holdDisMin:                                           float                         Defaults to 7. Reposition leg at this distance if neighbor legs are not already repositioning.
    EXAMPLE: holdDisMin: 10

  holdDisMax:                                           float                         Defaults to 16. Force reposition of leg at this distance. Repositions leg at this distance even if a neighboring leg is moving
    EXAMPLE: holdDisMax: 20

  holdDisMin_maxMovingLegs                              int                           Sets maximum amount of legs to check before applying distance holding
    EXAMPLE: holdDisMin_maxMovingLegs: 4

  hold_moveOnlyIfFurthest                               bool                          Defaults to true. When true, starting moving leg only if it is currently the furthest leg from where it should be.
    EXAMPLE: hold_moveOnlyIfFurthest: true

  holdDisMin_checkNeighbours                            bool                          Checks neighbors before applying distance holding
    EXAMPLE: holdDisMin_checkNeighbours: true

  hardLimit:                                            float                         Defaults to 50. Force leg to never go this far. Better to not be reached.
    EXAMPLE: hardLimit: 60

  estimatingPositionMultiplier                          float                         defaults to 1. Predicts were unit will be for leg placement based on unit speed.
    EXAMPLE: estimatingPositionMultiplier: 2

  hidden:                                               logic boolean                 When true, hides the arm/leg
    EXAMPLE: hidden: true

  image_end                                             file (image)         1.13     Sets the main arm end sprite
    EXAMPLE: image_end: rotor.png

  image_end_shadow                                      file (image)         1.13     Sets the shadow for the arm
    EXAMPLE: image_end_shadow: AUTO

  image_end_teamColors                                  bool                 1.13     When true, the arm end will apply team colors
    EXAMPLE: image_end_teamColors: true

  image_foot                                            file (image)         1.13     same as image_end, but acts as the foot for the leg
    EXAMPLE: image_foot: footR.png

  image_foot_shadow                                     file (image)                  Sets the shadow for the foot
    EXAMPLE: image_foot_shadow: AUTO

  image_middle                                          file (image)         1.13     Sets the arm image
    EXAMPLE: image_middle: NONE

  image_leg                                             file (image)                  Sets the leg image
    EXAMPLE: image_leg: legR.png

  draw_foot_on_top                                      bool                 1.13     Renders foot above leg
    EXAMPLE: draw_foot_on_top: true

  drawOverBody                                          bool                          Draw over body
    EXAMPLE: drawOverBody: false

  drawUnderAllUnits                                     bool                          Draw over all units
    EXAMPLE: drawUnderAllUnits: true

  drawDirOffset                                         float                         Rotates the foot/arm sprite to a specified direction
    EXAMPLE: drawDirOffset: 73

  dust_effect:                                          bool                          Spawns dust particles on each step.
    EXAMPLE: dust_effect: true

  spinRate                                              float                         Makes arm/leg spin, like idleSpin for turrets. Great for helicopters
    EXAMPLE: spinRate: 2

  favourOppositeSideNeighbours                          bool                          calculate neighbours with X 10 times closer than Y
    EXAMPLE: favourOppositeSideNeighbours: true

  drawLegWhenZoomedOut                                  bool                          For performance, defaults changes based on unit size
    EXAMPLE: drawLegWhenZoomedOut: false

  drawFootWhenZoomedOut                                 bool                          For performance, defaults changes based on unit size
    EXAMPLE: drawFootWhenZoomedOut: false

  liftingHeightOffset                                   int                  1.15p9   Often used with decals
    EXAMPLE: liftingHeightOffset: 20

  targetHeight                                          int                           Sets height of the leg
    EXAMPLE: targetHeight: 10

  targetHeightRelative                                  bool                          Apply height relative to unit's height
    EXAMPLE: targetHeightRelative: true

  resetAngle:                                           float                         Unused


====================================================================================================
SECTION: [attachment_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  x                                                     float                1.13.3   Sets the horiztontal position of the attachment
    EXAMPLE: x

  y                                                     float                1.13.3   Sets the vertical position of the attachment
    EXAMPLE: y

  height                                                float                1.13.3   Sets the elevation of the attachment
    EXAMPLE: height

  idleDir                                               int                  1.13.3   Sets the direction of the attachment when idle
    EXAMPLE: idleDir

  idleDirReversing                                      int                  1.14     Sets the direction of the attachment when the base unit is moving in reverse
    EXAMPLE: idleDirReversing

  isVisible                                             bool                 1.13.3   Defaults to true. If false, the attachment is hidden
    EXAMPLE: isVisible

  onCreateSpawnUnitOf                                   unit ref             1.13.3   Upon spawning the unit, specified attached unit is also spawned as attachment
    EXAMPLE: onCreateSpawnUnitOf

  isUnselectable                                        bool                 1.13.3   Defaults to false. When true, the player cannot click the attachment.
    EXAMPLE: isUnselectable

  canAttack                                             bool                          Defaults to true. Set to false to stop this attachment attacking.
    EXAMPLE: canAttack

  canBeAttackedAndDamaged                               bool                 1.13.3   When true, the attachment is vulnerable to attacks
    EXAMPLE: canBeAttackedAndDamaged

  deattachIfWantingToMove                               bool                 1.13.3   If the unit is ordered to move, it will detach. This includes waypoints from actions.
    EXAMPLE: deattachIfWantingToMove

  lockLegMovement                                       bool                 1.13.3   Locks the leg movement while attached.
    EXAMPLE: lockLegMovement

  keepAliveWhenParentDies                               bool                 1.13.3   Defaults to false
    EXAMPLE: keepAliveWhenParentDies: true

  setDrawLayerOnTop                                     bool                 1.13.3   Renders the attachment above the base unit
    EXAMPLE: setDrawLayerOnTop

  setDrawLayerOnBottom                                  bool                 1.13.3   Renders the attachment below the base unit
    EXAMPLE: setDrawLayerOnBottom

  addTransportedUnits                                   bool                 1.13.3   Often used with transport units, when true, attaches one of the unit passenger to this attachment slot
    EXAMPLE: addTransportedUnits

  lockRotation                                          bool                 1.13.3   When true, stops the attachment  from rotating
    EXAMPLE: lockRotation

  rotateWithParent                                      bool                 1.13.3   When true, the attachment rotates with the parent unit
    EXAMPLE: rotateWithParent

  resetRotationWhenNotAttacking                         bool                 1.13.3   Similar to shouldResetTurret:for turrets.
    EXAMPLE: resetRotationWhenNotAttacking

  prioritizeParentsMainTarget                           bool                 1.13.3   It will priotize targeting the main target. Defaults to true.
    EXAMPLE: prioritizeParentsMainTarget

  alwaysAllowedToAttackParentsMainTarget                bool                 1.13.3   Will always attack the parents main target.
    EXAMPLE: alwaysAllowedToAttackParentsMainTarget

  onParentTeamChangeKeepCurrentTeam                     bool                 1.14     Defaults false. If true attached units are not converted when parent changes team. Eg from [projectile]convertHitToSourceTeam
    EXAMPLE: onParentTeamChangeKeepCurrentTeam

  onConvertKeepExistingUnitInSameSlot                   bool                 1.14     When true, the attachment is retained on the same attachment slot when the parent is converted to another unit
    EXAMPLE: onConvertKeepExistingUnitInSameSlot

  unloadInCurrentPosition                               bool                 1.14     Defaults false. If true transported attached units are kept current attached location when unloading
    EXAMPLE: unloadInCurrentPosition

  keepWaypointsNeedingMovement                          bool                 1.14     Defaults false. If true attached units keep waypoints with movement even while they cannot move. Useful if they will be automatically deattached soon.
    EXAMPLE: keepWaypointsNeedingMovement

  smoothlyBlendPositionWhenExistingUnitAdded            bool                 1.14     
    EXAMPLE: smoothlyBlendPositionWhenExistingUnitAdded

  showAllActionsFrom                                    LogicBoolean         1.14     Show all actions of the units attached in the parent unit list when selected
    EXAMPLE: showAllActionsFrom

  createIncompleteIfParentIs:                           bool                 1.14     If parent hasn't been built, create attachment with the same built value. Links built values till attachment is complete. Useful for buildings built with nano.
    EXAMPLE: createIncompleteIfParentIs:

  redirectDamageToParent:                               bool                 1.14     Redirects damage done to this attachment to the parent instead of damaging itself directly
    EXAMPLE: redirectDamageToParent:

  redirectDamageToParent_shieldOnly:                    bool                 1.14     When enemies attack the attachment, all damage are redirected to the parent's shield
    EXAMPLE: redirectDamageToParent_shieldOnly:


====================================================================================================
SECTION: [action_NAME] / [hiddenAction_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  text                                                  string                        Labels the action. Supports dynamic text
    EXAMPLE: text: Catch Fish, text: Fire: %{self.resource.ammo}

  textPostFix:                                          string                        Text shown as suffix, useful with textAddUnitName to create text UI
    EXAMPLE: text: [  textPostFix: ]  textAddUnitName: unitRef self.attachment(slot="${slotId}")

  text_{LANG}                                           string                        Alternative text for different language. Use ISO 639-1 Language code on the {LANG} prefix. Supports dynamic text
    EXAMPLE: text_es: Fuego %{self.resource.ammo}

  description                                           string                        A display text when you select your unit's action, used to explain it's purpose. Supports dynamic text
    EXAMPLE: description: Fires shot on target area

  description_{LANG}                                    string                        Alternative description for different language. Use ISO 639-1 Language code on the {LANG} prefix. Supports dynamic text
    EXAMPLE: description_es: Disparos en el área objetivo

  displayType                                           list                          Affects how the action button and text is displayed. Options: none, rally, upgrade, queueUnit, building, action, infoOnly, infoOnlyNoBox, infoOnlyStockpile
    EXAMPLE: displayType: upgrade

  displayRemainingStockpile                             bool                          Queue is shown as number of times action can be triggered based on price. Use dynamic text on text as alternative.
    EXAMPLE: displayRemainingStockpile: true

  pos                                                   float                         Order action appears in UI. Merges with positions from canBuild buttons
    EXAMPLE: pos: 1

  iconImage                                             file (image)                  Sets a thumbnail image for the action button
    EXAMPLE: iconImage: fireShot.png

  iconExtraImage                                        file (image)         1.14     Drawn over top of icon image. Useful for upgrade icons, etc
    EXAMPLE: iconExtraImage: fireShotNo.png

  iconExtraColor                                        colour               1.14     Defaults to #64FFFFFF
    EXAMPLE: iconExtraColor: #ff0000

  iconExtraIsVisible                                    LogicBoolean         1.14     When the condition is met, the extras for icon is visible.
    EXAMPLE: iconExtraIsVisible: if self.resource.ammo < 1

  unitShownInUI                                         unitRef/unitType     1.13.3   Display this unit. (as if this action built this unit)
    EXAMPLE: eg: unitShownInUI: unitRef self.transporting(slot=0)  or  unitShownInUI: heavyTank

  guiBuildUnit                                          unitRef/unitType              Uses the UI similar when building structures. An alternative to fireTurretX on some cases
    EXAMPLE: guiBuildUnit: placeholderUnit

  setBuilt                                              float                1.14     Designates how built the unit is from a percentage of 0-100% with a number between 0 and 1.
    EXAMPLE: setBuilt: 0.5

  tags                                                  tags                 1.15p9   To be used with the withTag parameter for self.queueSize(withTag=x) and queueItemAdded and queueItemCanceled events
    EXAMPLE: tags: actionFire

  id                                                    string               1.15p11  Allow same/equivalent actions to be more easily connected when converting between units, to preserve queues, calldowns, etc. (Normally action order is used but can be unstable.)
    EXAMPLE: id: fireShot

  textAddUnitName                                       unitRef/unitType     1.14     Add this unit's name to this action's text
    EXAMPLE: eg: textAddUnitName: unitRef self.attachment(slot="1")

  descriptionAddFromUnit                                unitRef/unitType     1.14     Add this unit's description to this action's description
    EXAMPLE: descriptionAddFromUnit: builder

  descriptionAddUnitStats:                              unitRef/unitType     1.14     Add this unit's stats (eg HP, energy, resources) to this action's description
    EXAMPLE: descriptionAddUnitStats: unitRef self.parent()

  unitShownInUIWithHpBar                                bool                 1.14     default true, Only used when unitShownInUI is a unitRef
    EXAMPLE: unitShownInUIWithHpBar: true

  unitShownInUIWithProgressBar                          bool                 1.14     default true, Only used when unitShownInUI is a unitRef. Replaces HP bar if active
    EXAMPLE: unitShownInUIWithProgressBar: false

  alwaysSinglePress                                     bool                 1.14     Defaults false. When true no confirmation needed on mobile, when used with canPlayerCancel:false and allowMultipleInQueue:false will also hide the queue interface.
    EXAMPLE: alwaysSinglePress: true

  price                                                 resources                     The price of your action for the unit. Disables action if not available. Defaults to credits if unlabelled
    EXAMPLE: price: credits=5, energy=5, hp=100, shield=5, ammo=1

  isActive                                              LogicBoolean                  Defaults true. If false then action is disabled and shown in red in UI.
    EXAMPLE: isActive: true

  isVisible                                             LogicBoolean                  Defaults true. If false action is hidden from UI and disabled.
    EXAMPLE: isVisible: true

  isLocked                                              LogicBoolean                  Defaults false. If true action is disabled, and a lock icon is shown. Mostly used for no nuke game modes
    EXAMPLE: isLocked: if self.resource.ammo < 1

  isLockedMessage                                       LocaleString                  Shows the message when the isLocked's condition is met
    EXAMPLE: isLockedMessage: Not enough ammunition

  isLockedAlt                                           LogicBoolean         1.13.3   Another reason for this to be locked. Can just use OR on isLocked, but this allows a different message to be shown
    EXAMPLE: isLockedAlt: if numberOfUnitsInGame(withTag="factory") < 2

  isLockedAltMessage                                    LocaleString         1.13.3   Message for isLockedAlt
    EXAMPLE: isLockedAltMessage: Not enough factories!

  isLockedAlt2                                          LogicBoolean         1.13.3   Second isLocked alternative
    EXAMPLE: isLockedAlt2: if nearestUnit(withTag="explosive", withinRange="500", relation="own").hp() < 10

  isLockedAlt2Message                                   LocaleString         1.13.3   Message for isLockedAlt2
    EXAMPLE: isLockedAlt2Message: Explosive stockpile is heavily damaged.

  allowMultipleInQueue                                  bool                          When false makes it so only one action can be queued of this type (useful for keeping actions with conditions from being spammed)
    EXAMPLE: allowMultipleInQueue: true

  onlyOneUnitAtATime                                    bool                          When action is picked in UI, only one unit selected with get this action. Defaults to false.
    EXAMPLE: onlyOneUnitAtATime: true

  isGuiBlinking                                         LogicBoolean         1.13.3   Flashes in UI to draw attention to it. Might be annoying if used often, recommended only for temporarily states/messages
    EXAMPLE: isGuiBlinking: true

  isAlsoViewableByAllies                                bool                 1.14     Allows ally players to see actions from this unit, useful for showing stats to other players (eg missile count, items collected)
    EXAMPLE: isAlsoViewableByAllies: true

  isAlsoViewableByEnemies                               bool                 1.14     Allows enemy players to see actions from this unit, useful for showing stats to other players (eg missile count, items collected)
    EXAMPLE: isAlsoViewableByEnemies: true

  ai_isHighPriority                                     LogicBoolean         1.13.3   Use this for faction selection actions or other high priority actions such as building high priority units
    EXAMPLE: ai_isHighPriority: true

  ai_isDisabled                                         LogicBoolean                  Defaults false. Stop AI using this action. (Note when ai_isHighPriority is true this might be ignored)
    EXAMPLE: ai_isDisabled: false

  ai_considerSameAsBuilding                             bool                 1.13.3   Be careful with
    EXAMPLE: ai_considerSameAsBuilding: true

  autoTriggerOnEvent:                                   event                1.13.3 - 1.15p9 Action will be triggered when an event is happening on a particular unit. Options: created, completeAndActive, destroyed, killedAnyUnit, queuedUnitFinished, queueItemAdded(withActionTag="#"), queueItemCancelled(withActionTag="#"), teleported, touchTargetSuccess, newWaypointGivenByPlayer, teamChanged, transportingNewUnit, transportUnloadedOrRemovedUnit, tookDamage(withTag="#"), newMessage(withTag="#"), enteredTransport, leftTransport, attachmentRemoved. withTag parameter for tookDamage uses tag from projectile and it is optional. withTag parameter for newMessage uses message tags.
    EXAMPLE: autoTriggerOnEvent: queueItemCancelled(withActionTag="actionFire")

  autoTriggerOnEventRecursionLimit                      int                  1.15     Defaults to 1. Prevents loops, useful with triggering itself so it doesn't infinitely triggers, good for repeating action effect on event
    EXAMPLE: autoTriggerOnEventRecursionLimit: 4

  autoTrigger                                           LogicBoolean                  When true triggers the effects of this action instantly (ignoring price, isActive, isVisible, buildSpeed, etc)
    EXAMPLE: autoTrigger: if self.overWater(), autoTrigger: if self.customTimer(laterThanSeconds=5)

  autoTriggerCheckRate                                  enum                          options: everyFrame (default), every4Frames, every8Frames. This overrides autoTriggerCheckRate set on [core] Note: all triggers regardless of check rate are checked when first created and after an auto trigger cooldown. Note: Adding [core]autoTriggerCheckRate:every8Frames to all-units.template could have a large performance boost for mods with complex autoTriggers.
    EXAMPLE: autoTriggerCheckRate:every8Frames

  buildSpeed                                            time                          Sets how fast the action has to be queued before doing the action behaviors
    EXAMPLE: buildSpeed: 5s

  highPriorityQueue                                     bool                          Defaults to false. If true this action skips all other low priority actions in queue. Useful for fireTurret actions.
    EXAMPLE: highPriorityQueue: true

  canPlayerCancel                                       bool                          Defaults to true. When false, players cannot cancel this particular action.
    EXAMPLE: canPlayerCancel: false

  whenBuilding_cannotMove                               bool                          Stops unit moving while action is being applied. Useful for deploy like actions.
    EXAMPLE: whenBuilding_cannotMove: true

  whenBuilding_playAnimation                            animation ref                 Plays a specified animation while the action is queued.
    EXAMPLE: whenBuilding_playAnimation: firePrepare

  whenBuilding_rotateTo                                 float                         Rotate unit body to this direction when action is in active queue.
    EXAMPLE: whenBuilding_rotateTo: 45

  whenBuilding_rotateTo_orBackwards                     bool                          If true allow rotation in 180 degrees from whenBuilding_rotateTo when this is a smaller angle
    EXAMPLE: whenBuilding_rotateTo_orBackwards: true

  whenBuilding_rotateTo_waitTillRotated                 bool                          Pause action queue till rotation is finished
    EXAMPLE: whenBuilding_rotateTo_waitTillRotated: true

  whenBuilding_temporarilyConvertTo                     unit ref                      Convert to another unit while action is in active queue. Note: actions from the original unit will be kept
    EXAMPLE: whenBuilding_temporarilyConvertTo: cannon_uberState

  whenBuilding_temporarilyConvertTo_keepFields          fields               1.15     Don't change these fields when using whenBuilding_temporarilyConvertTo (both to and from)
    EXAMPLE: whenBuilding_temporarilyConvertTo_keepFields: maxHp, maxEnergy, moveSpeed

  whenBuilding_triggerAction                            action ref                    While action is queued, another action is triggered.
    EXAMPLE: whenBuilding_triggerAction: spawnMinions

  whenBuilding_rotateTo_aimAtActionTarget               bool                 1.14     While action is queued, the unit is rotated to the target. Often used with fireTurretX actions
    EXAMPLE: whenBuilding_rotateTo_aimAtActionTarget: true

  whenBuilding_rotateTo_rotateTurretX                   turret ref           1.14     While action is queued, a specified turret is aimed at the target. Often used with fireTurretX actions
    EXAMPLE: whenBuilding_rotateTo_rotateTurretX: cannon

  spawnEffectsOnQueue                                   effect ref                    Effects to spawn at unit when action is first added to queue
    EXAMPLE: spawnEffectsOnQueue: CUSTOM:steam

  playSoundToPlayerOnQueue                              sound ref                     Global sound to play to unit's player only when action is first added to queue
    EXAMPLE: playSoundToPlayerOnQueue: eva_building.ogg

  lockCameraToUnitOnQueue                               dynamic unit ref     1.16     Only effects player that triggered the action. Locks the camera on this unit and stops if the player tries to move the camera. If multiple units are triggered, the camera won't lock at all.
    EXAMPLE: lockCameraToUnitOnQueue: lastDamagedBy

  addSelectedUnitOnQueue                                dynamic unit ref     1.16     Only effects player that triggered action. Modifies the current active player selection to include in addition the unit references.

  setSelectedUnitOnQueue                                dynamic unit ref     1.16     Only effects player that triggered action. Modifies the current active player selection to become the unit references.

  requireConditional                                    LogicBoolean                  Skip all effects of this action if this evaluates to false
    EXAMPLE: requireConditional: if self.resource.mass < 300

  convertTo                                             unit ref             1.13.3   Convert your unit into another unit. properties are preserved.
    EXAMPLE: convertTo: fishLevel2

  convertTo_keepCurrentTags                             bool                 1.14     Keep current and temporarily tags and ignores default tags on convertTo target.
    EXAMPLE: convertTo_keepCurrentTags: true

  convertTo_keepCurrentFields                           fields               1.15     Don't change these fields when converting, useful with setUnitStats (Allowed fields: maxHp, maxShield, shieldRegen, maxEnergy, armour, mass, shootDelayMultiplier, moveSpeed, maxAttackRange.)
    EXAMPLE: convertTo_keepCurrentFields: armour, maxEnergy, maxHp

  addEnergy                                             addEnergy                     Adds energy to unit. Has no effect unless energyMax is set. (Same as addResources: energy=X)
    EXAMPLE: addEnergy: 10

  addResources                                          resources                     Add these resources when action finishes.
    EXAMPLE: addResources: credits=5, energy=-5, hp=-100, shield=5, ammo=1

  addResourcesScaledByAIHandicaps                       resources                     Same as addResources, but increased or decreased depending on AI difficulty level
    EXAMPLE: addResourcesScaledByAIHandicaps: credits=5, hp=-100

  addResourcesWithLogic                                 dynamic resources    1.15     Like addResources but allows logic to be used for the resource value
    EXAMPLE: addResourcesWithLogic: hp = select( self.parent.energy>5, 10, 20 )

  setResourcesWithLogic                                 dynamic resources    1.15     Sets target resources to this value instead of adding. Becareful with global resources.
    EXAMPLE: setResourcesWithLogic: hp=self.parent.hp - 10, energy = self.energy / 2

  deleteSelf                                            bool                          Remove self with no explosions or sounds
    EXAMPLE: deleteSelf: true

  resetCustomTimer                                      LogicBoolean                  Reset timer used with self.customTimer()
    EXAMPLE: resetCustomTimer: true

  setBodyRotation                                       int                  1.13.3   Rotates the unit to a particular direction. Supports dynamic values
    EXAMPLE: setBodyRotation: 270, setBodyRotation: directionBetween(self, customTarget1)

  setSpeed                                              dynamic vec2         1.16     Sets the units current speed

  addSpeed                                              dynamic vec2         1.16     Adds to the units current speed

  setUnitStats                                          fields values        1.15     Allows changing of a select number of fields dynamically without converting. Supports =/+=/-=, with dynamic maths/logic. Changeable fields: maxHp, hp, maxShield, shield, shieldRegen, maxEnergy, energy, armour, mass, shootDelayMultiplier, shootDamageMultiplier, moveSpeed, maxTurnSpeed, maxAttackRange, fogOfWarSightRange, nanoRange, selfRegenRate, targetHeight, nanoFactorySpeed. (targetHeight adjust the unit's height gradually relative to its current height, unlike setting it with setHeight)
    EXAMPLE: setUnitStats: maxShield += 20, moveSpeed += 0.1

  resetUnitStats                                        boolean              1.15     Reset changes made by setUnitStats to base values
    EXAMPLE: resetUnitStats: true

  setUnitMemory                                         key value pairs      1.15     Change this unit's memory, values can be set with logic. Memory must first be defined with defineUnitMemory
    EXAMPLE: setUnitMemory: """
  customText=memory.customText+'hello',
  nukeActive=true, 
  nextTarget=self.attacking.nearestUnit(withinRange=300, withTag='x', relation='enemy')
"""

  setHeight                                             logicNumber          1.15     Changes unit height based on this height value
    EXAMPLE: setHeight: parent.height+5

  setCustomTarget1:                                     unit ref             1.15     Used for unit linking without requiring unit memories or markers. It is the unit that built this by default.
    EXAMPLE: setCustomTarget1: self.parent

  setCustomTarget2:                                     unit ref             1.15     Like setCustomTarget1 and with the same use. It is none by default.
    EXAMPLE: setCustomTarget2: lastDamagedBy

  sendMessageTo:                                        unit ref             1.15     Sends a message to a targeted unit
    EXAMPLE: sendMessageTo: unitref nearestUnit(withinRange=100, withTag="fish").

  sendMessageWithTags:                                  Message Tag          1.15     Useful for message detection in an autoTriggerOnEvent event, eg autoTriggerOnEvent:newMessage(withTag='xyz')
    EXAMPLE: sendMessageWithTags: hitZone

  sendMessageWithData                                   key-value            1.15     The data that will be sent to the targeted unit. Allows multiple key-value pairs with any dynamic data type, use eventData() to read this data in the event
    EXAMPLE: sendMessageWithData: fish="nice!", cat=self.activeWaypointTarget, amount=4, xyz=memory.something

  refundAllQueuedItems                                  boolean              1.15     Refunds the spent price in the queue of a specific action; Includes set flags in price
    EXAMPLE: refundAllQueuedItems: true

  removeAllQueuedItemsWithoutRefund                     boolean              1.15     Similar to refundAllQueuedItems, but does not refund
    EXAMPLE: removeAllQueuedItemsWithoutRefund: true

  alsoTriggerAction                                     action refs                   Trigger to results of another action as well. Ignores action's requirements.
    EXAMPLE: alsoTriggerAction: addCredits, playSound

  alsoQueueAction                                       action refs                   Adds another action into the normal unit's queue. Ignores action's requirements
    EXAMPLE: alsoQueueAction: spawnMinions

  alsoTriggerOrQueueActionConditional                   LogicBoolean                  Defaults true. alsoTriggerAction and alsoQueueAction are ignored if this works out to be false.
    EXAMPLE: alsoTriggerOrQueueActionConditional: false

  alsoTriggerOrQueueActionWithTarget                    unitref              1.15p9   Changes the target of the triggered action, normally defaults to the current action target. Effects things like fireTurretXAtGround, spawnUnits, thisActionTarget(), etc
    EXAMPLE: alsoTriggerOrQueueActionWithTarget: lastDamagedBy

  alsoTriggerActionRepeat                               logicNumber          1.15p11  Repeats the alsoTriggerAction call, thisActionIndex changed on each repeat - Useful to create loops or work with arrays
    EXAMPLE: alsoTriggerActionRepeat: 5

  otherwiseTriggerAction                                action refs          1.16     Triggers a different action if autoTrigger or requireConditionals return false. (Serves as the "else" in a generic programming syntax)

  playSoundAtUnit                                       sound ref                     Local sound to play when action finishes
    EXAMPLE: playSoundAtUnit: engineStart.ogg

  playSoundGlobally                                     sound ref                     Global sound to play to all players in game
    EXAMPLE: playSoundGlobally: hornWarn.ogg

  playSoundToPlayer                                     sound ref                     Global sound to play to unit's player only
    EXAMPLE: playSoundToPlayer: confirm.wav

  fireTurretXAtGround                                   turret ref                    When action finishes fire target turret at point on ground, bypasses canShoot rules in turret.
    EXAMPLE: fireTurretXAtGround: nukeSilo

  fireTurretXAtGround_withOffset:                       point                         If not set player targets the ground with GUI, if a point is set this step is skipped
    EXAMPLE: fireTurretXAtGround_withOffset: 0,0

  fireTurretXAtGround_withProjectile:                   projectile ref                Used with fireTurretXAtGround. Defaults to target turret's normal projectile.
    EXAMPLE: fireTurretXAtGround_withProjectile: nuke

  fireTurretXAtGround_withTarget                        unitref              1.15     Fires a turret aimed at the location of the indicated unit or marker
    EXAMPLE: fireTurretXAtGround_withTarget: lastDamagedBy

  fireTurretXAtGround_count                                                  1.13.3   Number of projectiles to fire. Defaults to 1
    EXAMPLE: fireTurretXAtGround_count: 3

  fireTurretXAtGround_onlyOverPassableTileOf                                 1.13.3   Only allow tiles crossable by this movement type to be selected (e.g., LAND,BUILDING,WATER,HOVER)
    EXAMPLE: fireTurretXAtGround_onlyOverPassableTileOf: HOVER

  fireTurretXAtGround_showGuideDecals                   decal refs           1.15p11  Draws decals at the target location. Recommend setting up decals with layer: inactive
    EXAMPLE: fireTurretXAtGround_showGuideDecals: strikeZone300

  spawnUnits                                            unitref              1.13.3   Spawn units at action's target. See 'Spawn units line' section in this doc for details.
    EXAMPLE: eg: spawnUnits: heavyTank, tank*5, hoverTank(offsetX=10)

  produceUnits                                          unitref              1.13.3   Like spawnUnits but unit exits as if it was produced normally, and gets a move away waypoint
    EXAMPLE: produceUnits: builder*4, plasmaTank*8

  spawnEffects                                          effect ref                    Effects to spawn at unit
    EXAMPLE: spawnEffects: CUSTOM:puff

  offsetSelfAbsolute:                                   point3d              1.14     Offsets unit position absolutely by this point. Format: [x,y,height]
    EXAMPLE: offsetSelfAbsolute: 0, 0, 40

  teleportTo                                            unit ref/marker      1.15p11  Changes unit position to this position. Great alternative to fireTurretX with teleporting projectile
    EXAMPLE: teleportTo: memory.lastLocation

  addUnitsIntoTransport                                 unitTypes                     Creates and add units into transport, use self.transportingCount() to check for space before adding
    EXAMPLE: addUnitsIntoTransport: tank*3, heavyTank(neutralTeam=true)

  transportTargetNow                                    unit ref             1.15     {Currently broken in 1.15 - don't use} Instantly tries to transports existing units on the map into this transport. Might fail if rules don't allow this unit to be transported.

  forceTransportTargetNow                               dynamic unit ref     1.16     Bypasses all transport checks even if the target is not even a transport. Be careful with usage

  deleteNumUnitsFromTransport                           int                           Removes a specified amount of cargo units
    EXAMPLE: deleteNumUnitsFromTransport: 2

  deleteNumUnitsFromTransport_onlyWithTags              string(s)            1.13.3   Removes a specified amount of cargo units, but only those with specified tags
    EXAMPLE: deleteNumUnitsFromTransport_onlyWithTags: cheapStuff

  startUnloadingTransport                               bool                 1.13.3   Unloads all cargo units normally
    EXAMPLE: startUnloadingTransport: true

  forceUnloadTransportNow                               bool                 1.13.3   For unload all units, or slot targeted by forceUnloadTransportNow_onlyOnSlot. Unloads even if no space or overwater, etc. Take note that the payloads are unloaded on the exit coordinates of the units (exit_x and exit_y in [core] section)
    EXAMPLE: forceUnloadTransportNow: true

  forceUnloadTransportNow_onlyOnSlot                    int                  1.14     Focuses the force unload to a specific slot
    EXAMPLE: forceUnloadTransportNow_onlyOnSlot: drop1

  clearAllWaypoints                                     bool                 1.13.3   Clears all waypoints, be careful not to annoy players by removing their orders, prepending waypoints is often better
    EXAMPLE: clearAllWaypoints: true

  clearActiveWaypoint                                   bool                 1.13.3   Clears only the current waypoint
    EXAMPLE: clearActiveWaypoint: true

  addWaypoint_type                                      enum                          Adds a waypoint with a specific purpose. Options: move, attackMove, guard, loadInto, loadUp, attack, reclaim, repair, touchTarget, build, follow, setPassiveTarget
    EXAMPLE: addWaypoint_type: move

  addWaypoint_unitType                                  unitTypes            1.13.3   Only for use with addWaypoint_type:build
    EXAMPLE: addWaypoint_unitType: turret

  addWaypoint_prepend                                   bool                 1.13.3   Add to the start of the waypoint queue or the end
    EXAMPLE: addWaypoint_prepend: false

  addWaypoint_triggerActionIfFailed                     actions              1.13.3   If target_nearestUnit fails to find a match so waypoint cannot be added then trigger this action
    EXAMPLE: addWaypoint_triggerActionIfFailed: retreat

  addWaypoint_triggerActionIfMatched                    actions              1.14     Triggers an action if a waypoint is legal
    EXAMPLE: addWaypoint_triggerActionIfMatched: VIP_target

  addWaypoint_maxTime                                   time                 1.13.3   Automatically remove this waypoint if it has been active for longer than this time.
    EXAMPLE: addWaypoint_maxTime: 20s

  addWaypoint_target_nearestUnit_tagged                 tags                 1.13.3   Puts the waypoint to a nearest unit with specific tags
    EXAMPLE: addWaypoint_target_nearestUnit_tagged: assault, mechanized

  addWaypoint_target_nearestUnit_team                   relation             1.13.3   Puts the waypoint to a nearest unit with a specific relation to the player's unit. Options: own, neutral, allyNotOwn, ally, enemy, any, notOwn
    EXAMPLE: addWaypoint_target_nearestUnit_team: ally

  addWaypoint_target_nearestUnit_maxRange               float                1.13.3   Puts the waypoint to a nearest unit within the maximum range specified.
    EXAMPLE: addWaypoint_target_nearestUnit_maxRange: 2000

  addWaypoint_target_mapMustBeReachable                 bool                 1.13.3   Puts the waypoint only if the waypoint target is reachable by the unit (e.g., if a unit cannot cross two islands, the waypoint is not placed.)
    EXAMPLE: addWaypoint_target_mapMustBeReachable: true

  addWaypoint_target_fromReference                      unit ref             1.15     Puts the waypoint to a marker or a reference unit
    EXAMPLE: addWaypoint_target_fromReference: self.memory.lastDock

  addWaypoint_position_offsetFromSelf                   point                1.13.3   Puts the waypoint to an absolute coordinate
    EXAMPLE: addWaypoint_position_offsetFromSelf:

  addWaypoint_position_fromAction                       bool                 1.13.3   Puts the waypoint taken from an action that triggered this action
    EXAMPLE: addWaypoint_position_fromAction: fireShot

  addWaypoint_position_randomOffsetFromSelf             point                1.13.3   Puts the waypoint to a random relative coordinate
    EXAMPLE: addWaypoint_position_randomOffsetFromSelf:

  addWaypoint_position_relativeOffsetFromSelf           point                1.14     Puts the waypoint to a relative coordinate
    EXAMPLE: addWaypoint_position_relativeOffsetFromSelf: 10, 20

  addWaypoint_target_randomUnit_tagged                  tags                 1.14p6   Puts the waypoint to a random unit with specific tags
    EXAMPLE: addWaypoint_target_randomUnit_tagged: chargers, strikers

  addWaypoint_target_randomUnit_team                    relation             1.14p6   Puts the waypoint to a random unit with a specific relation to the player's unit. Options: own, neutral, allyNotOwn, ally, enemy, any, notOwn
    EXAMPLE: addWaypoint_target_randomUnit_team: any

  addWaypoint_target_randomUnit_maxRange                int                  1.14p6   Puts the waypoint to a random unit within the maximum range specified.
    EXAMPLE: addWaypoint_target_randomUnit_maxRange: 1000

  addAllActionCooldownsTime                             time                 1.14     Adds cooldown on all actions, including canBuilds
    EXAMPLE: addAllActionCooldownsTime: 4s

  addActionCooldownTime                                 time                 1.14     Player cannot use action again for this amount of time
    EXAMPLE: addActionCooldownTime: 20s

  addActionCooldownApplyToActions                       action ids           1.14     Sets addActionCooldownTime's target. Defaults to this action.
    EXAMPLE: addActionCooldownApplyToActions: retreat, transformFins

  clearAllActionCooldowns                               bool                 1.14     Removes all existing cooldowns on all buttons
    EXAMPLE: clearAllActionCooldowns: true

  playAnimation                                         animation id         1.13.3   Plays animation when the action is triggered
    EXAMPLE: playAnimation: engageThrusters

  playAnimationIfNotPlaying                             bool                 1.13.3   Don't restart animation if this animation is already playing
    EXAMPLE: playAnimationIfNotPlaying: true

  finishPlayingLastAnimation                            bool                 1.13.3   Finish last animation, including blend out
    EXAMPLE: finishPlayingLastAnimation: true

  stopLastAnimation                                     bool                 1.13.3   Stop last animation, skipping blend out
    EXAMPLE: stopLastAnimation: true

  switchToNeutralTeam                                   boolean              1.13.3   Change team to neutral. This team is allied to all other teams. Will be captured by nearby units unless [core]stayNeutral:true is used
    EXAMPLE: switchToNeutralTeam: false

  switchToAggressiveTeam                                boolean              1.13.3   Change to a built-in team that is aggressive to all other teams. Does not get captured.
    EXAMPLE: switchToAggressiveTeam: true

  switchToTeam                                          logicNumber          1.15     Team id to switch to. Starts at 0. (but -1 for a neutral team, -2 for aggressive Team). Also supports dynamic values
    EXAMPLE: switchToTeam: 2, switchToTeam: lastDamagedBy.teamId

  takeResources                                         customPrice          1.13.3   Resources to take (required to use take resources). And at-least 1 include key is needed.
    EXAMPLE: takeResources: hp=5, gold=10

  takeResources_includeUnitsInTransport                 bool                 1.13.3   Includes cargo units in taking resources
    EXAMPLE: takeResources_includeUnitsInTransport: true

  takeResources_includeParent                           bool                 1.13.3   Include attachment parent or transport parent
    EXAMPLE: takeResources_includeParent: true

  takeResources_includeReference                        unit ref             1.15     Includes referenced units in taking resources
    EXAMPLE: takeResources_includeReference: self.lastDamagedBy

  takeResources_includeUnitsWithinRange                 float                1.13.3   Includes all units within the specified range
    EXAMPLE: takeResources_includeUnitsWithinRange: 300

  takeResources_includeUnitsWithinRange_team            TeamRelation         1.13.3   Used with includeUnitsWithinRange, defaults to own. Can be:  own|ally|allyNotOwn|enemy|neutral|any
    EXAMPLE: takeResources_includeUnitsWithinRange_team: own

  takeResources_excludeUnitsWithoutTags                 tags                 1.13.3   Includes all units without the specified tags in taking resources
    EXAMPLE: takeResources_excludeUnitsWithoutTags: truck, tanker

  takeResources_excludeUnitsWithTheseResources          customPrice          1.13.3   Excludes all units with custom prices in them
    EXAMPLE: takeResources_excludeUnitsWithTheseResources: oil, juice, sauce

  takeResources_excludeUnitsWithoutAllResources         bool                 1.13.3   Defaults to true.
    EXAMPLE: takeResources_excludeUnitsWithoutAllResources: true

  takeResources_triggerActionIfAnyCollected             action refs          1.13.3   Triggers an action if any amount of resources is collected in the current collection action
    EXAMPLE: takeResources_triggerActionIfAnyCollected: purify

  takeResources_triggerActionIfNoneCollected            action refs          1.13.3   Triggers an action if no amount of resources is collected in the current collection action
    EXAMPLE: takeResources_triggerActionIfNoneCollected: callOtherTanker

  takeResources_triggerActionForEach                    action refs          1.15p11  Calls this action for each unit found by takeResource with the unit as thisActionTarget, and thisActionIndex counting up from zero.
    EXAMPLE: takeResources_triggerActionForEach: manufacture

  takeResources_discardCollected                        bool                 1.13.3   Just take resources from targets, don't add(or remove) to self
    EXAMPLE: takeResources_discardCollected: false

  takeResources_keepResourcesOnTarget                   bool                 1.13.3   Don't add/remove resource from target. This clones resources. Use with takeResources_discardCollected and takeResources_triggerActionIfAnyCollected to make a resource detector.
    EXAMPLE: takeResources_keepResourcesOnTarget: true

  takeResources_maxUnits                                int                           Defaults to 1. Takes resources from specified amount of units
    EXAMPLE: takeResources_maxUnits: 10

  takeResources_searchOnly                              bool                 1.15     Shortcut for maxUnits: 200, discardCollected: true, keepResourcesOnTarget: true to detect resources only
    EXAMPLE: takeResources_searchOnly: true

  takeResources_directTransferStoppingAtZero            bool                 1.14     If less resources on target than transfer amount, only remaining resources will be transfered. Doesn't support use with some other takeResources_* keys
    EXAMPLE: takeResources_directTransferStoppingAtZero: true

  convertResource_from                                  customResource       1.13.3   Name of custom resource to take from
    EXAMPLE: convertResource_from: juice

  convertResource_to                                    customResource       1.13.3   Name of custom resource to give to
    EXAMPLE: convertResource_to: sauce

  convertResource_minAmount                             float                1.13.3   Skip if less than this amount in 'from'. Defaults to 0. Likely not needed for most use cases
    EXAMPLE: convertResource_minAmount: 10

  convertResource_maxAmount                             float                1.13.3   Max amount to transfer between 'from' and 'to'
    EXAMPLE: convertResource_maxAmount: 100

  convertResource_multiplyAmountBy                      float                1.13.3   Defaults to 1. Amount to multiply when adding on 'to' (does not effect amount taken on 'from')
    EXAMPLE: convertResource_multiplyAmountBy: 1.5

  resourceAmount                                        customResource       1.13.3   Name of custom resource to set with the below 3 keys. All keys are optional, and can be used together.
    EXAMPLE: resourceAmount: oil

  resourceAmount_setValue                               float                1.13.3   Absolute value to set this resource to, ignores current value of resource. Skipped by default
    EXAMPLE: resourceAmount_setValue: 20

  resourceAmount_addOtherResource                       customResource       1.13.3   Name of another custom resource to add to this on. Can be used without resourceAmount_setValue, to just add resources. Or with resourceAmount_setValue:0 to copy a resource value.
    EXAMPLE: resourceAmount_addOtherResource: juice

  resourceAmount_multiplyBy                             float                1.13.3   Defaults to 1. Multiple the current or new value by
    EXAMPLE: resourceAmount_multiplyBy: 2

  attachments_addNewUnits                               unit types           1.13.3   Adds specified units to attachments
    EXAMPLE: attachments_addNewUnits: coreDefense*3

  attachments_deleteNumUnits                            int                  1.13.3   Removes a specified amount of units attached
    EXAMPLE: attachments_deleteNumUnits: 3

  attachments_onlyOnSlots                               attachment ids       1.13.3   Restrict attachments_* actions to these attachments
    EXAMPLE: attachments_onlyOnSlots: drop1, drop2, drop3

  attachments_unload                                    bool                 1.14     Unload all attachments. Can be used with attachments_onlyOnSlots. Same as unloading transported units
    EXAMPLE: attachments_unload: true

  attachments_disconnect                                bool                 1.14     Disconnect all attachments in the place they are right now. Can be used with attachments_onlyOnSlots.
    EXAMPLE: attachments_disconnect: true

  disconnectFromParent                                  bool                 1.13.3   Disconnects this unit from parent's attachment slot
    EXAMPLE: disconnectFromParent: true

  temporarilyAddTags                                    tags                 1.13.3   Add tag to this unit until it is converted or reset (unless convertTo_keepCurrentTags is used)
    EXAMPLE: temporarilyAddTags: emptyJuice, fullSauce

  temporarilyRemoveTags                                 tags                 1.13.3   Remove tag from this unit until it is converted or reset (unless convertTo_keepCurrentTags is used)
    EXAMPLE: temporarilyRemoveTags: fullJuice, emptySauce

  resetToDefaultTags                                    bool                 1.13.3   Reset to standard tags
    EXAMPLE: resetToDefaultTags: true

  addGlobalTeamTags                                     tags                 1.13.3   Add a tag to player's team. Use with self.globalTeamTags() to create unlocks and upgrades. Unique tags are best to not conflict with other mods.
    EXAMPLE: addGlobalTeamTags: upgrade_energized, upgrade_research2

  removeGlobalTeamTags                                  tags                 1.13.3   Remove a tag from player's team.
    EXAMPLE: removeGlobalTeamTags: buff_immune

  showMessageToPlayer                                   string               1.13.3   Sends a message to the player controlling the unit
    EXAMPLE: showMessageToPlayer: There is a hidden enemy near your defenses

  showMessageToPlayer_{LANG}                            string               1.13.3   Note: This format is support on nearly all strings that show to player even when reference doesn't show it. Use ISO 639-1 Language Code on the {LANG} placeholder
    EXAMPLE: showMessageToPlayer_fil: May nakatagong kalaban na malapit sa mga depensa mo

  showMessageToAllPlayers                               string               1.13.3   Sends a message to all players
    EXAMPLE: showMessageToAllPlayers: %{self.playerName} has captured a point

  showMessageToAllEnemyPlayers                          string               1.14     Sends a message to all enemy players only
    EXAMPLE: showMessageToAllEnemyPlayers: Team %{self.playerName} has %{self.resource.gold}

  showQuickWarLogToPlayer                               string               1.13.3   Sends a Quick War Log message to the player controlling the unit (in the lower lef)
    EXAMPLE: showQuickWarLogToPlayer: Unit decloaked

  showQuickWarLogToAllPlayers                                                1.13.3   Sends a Quick War Log message to all players (in the lower lef)
    EXAMPLE: showQuickWarLogToAllPlayers: 500 oil sold to allied market

  debugMessage                                                               1.13.3   Only shows in Sandbox with Debug mode on.
    EXAMPLE: debugMessage: [action log] unit launch on %{thisActionTarget.x}, %{thisActionTarget.y}


====================================================================================================
SECTION: [effect_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  drawType                                              normal|displacement  1.15p9   Defaults to normal. Changes how effect is rendered.  Options: normal|displacement   (displacement only shows when shader effects are turned on)
    EXAMPLE: drawType: normal

  life                                                  float                1.13     Defaults 200. Time till effect is removed. Set low as possible to reduce effect overhead.
    EXAMPLE: life: 70

  lifeRandom                                            float                1.14     Random offset life by +/- this value
    EXAMPLE: lifeRandom: 12

  alsoEmitEffects                                       effect ref           1.13     Create more effects when created, useful for meta-effects. Note: other 'alsoEmitEffects' on created effects are ignored.
    EXAMPLE: alsoEmitEffects: CUSTOM:extraSparks*2

  alsoEmitEffectsOnDeath                                effect ref           1.14     Create these effects when life runs out.
    EXAMPLE: alsoEmitEffectsOnDeath: CUSTOM:finalPuff

  ifSpawnFailsEmitEffects                               effect ref                    If 'spawnChance' for this effects fails then emit these effects instead
    EXAMPLE: ifSpawnFailsEmitEffects: CUSTOM:greenSparks*4

  alsoPlaySound                                         sound ref            1.13     Plays sound upon spawning the effect. Use OGG or WAV audio files. Appending a colon with a float value sets the volume
    EXAMPLE: alsoPlaySound: meow.wav:0.5

  createWhenOffscreen                                   bool                 1.13     Defaults false. When true, effect is created when the unit is offscreen
    EXAMPLE: createWhenOffscreen: true

  createWhenZoomedOut                                   bool                 1.13     Defaults true. Effect is created even when the map is zoomed out. Set to false for improving performance
    EXAMPLE: createWhenZoomedOut: false

  createWhenOverLiquid                                  bool                 1.13     Defaults true. Effect is created when the unit is over water
    EXAMPLE: createWhenOverLiquid: false

  createWhenOverLand                                    bool                 1.13     Defaults true. Effect is created when the unit is over land
    EXAMPLE: createWhenOverLand: false

  spawnChance                                           float                1.13     Default 1. If less than 1 effect only has a random chance of being created
    EXAMPLE: spawnChance: 1

  showInFog                                             bool                 1.13     Default false. When true, effect is still visible on fog of war
    EXAMPLE: showInFog: true

  delayedStartTimer                                     float                1.13     Hide for x time before showing and updating effect.
    EXAMPLE: delayedStartTimer: 2s

  liveAfterAttachedDies                                 bool                 1.13     Defaults false when attachedToUnit is being used
    EXAMPLE: liveAfterAttachedDies: false

  priority                                              string               1.13     Defaults to high. verylow/low/high/veryhigh/critical. Takes effect when too many effects are being shown at once.
    EXAMPLE: priority: critical

  attachedToUnit                                        bool                 1.13     Attach to unit or projectile that created this effect. Will move with this object. Useful for thrust effects
    EXAMPLE: attachedToUnit: true

  alwayStartDirAtZero                                   bool                 1.13     Ignore source/attached unit dir
    EXAMPLE: alwayStartDirAtZero: true

  atmospheric                                           bool                 1.13     Apply drag to slow this effect down and add small wind effects
    EXAMPLE: atmospheric: true

  physics                                               bool                 1.13     Fall to ground and bounces. Needs height to take effect.
    EXAMPLE: physics: true

  physicsGravity                                        float                1.13     Defaults to 1. height speed acceleration when physics: true
    EXAMPLE: physicsGravity: 0.5

  xOffsetRelative                                       float                1.13     Offset starting effect position. Relative to direction of attached turret, projectile, unit
    EXAMPLE: xOffsetRelative: 2

  yOffsetRelative                                       float                1.13     Offset starting effect position. Relative to direction of attached turret, projectile, unit
    EXAMPLE: yOffsetRelative: 2

  xOffsetRelativeRandom                                 float                1.13     Random offset by +/- this value
    EXAMPLE: xOffsetRelativeRandom: 4

  yOffsetRelativeRandom                                 float                1.13     Random offset by +/- this value
    EXAMPLE: yOffsetRelativeRandom: 4

  xOffsetAbsolute                                       float                1.13     Offset starting effect by position ignoring direction of attached turret, projectile, unit
    EXAMPLE: xOffsetAbsolute: 2

  yOffsetAbsolute                                       float                1.13     Offset starting effect by position ignoring direction of attached turret, projectile, unit
    EXAMPLE: yOffsetAbsolute: 2

  xOffsetAbsoluteRandom                                 float                1.13     Random offset by +/- this value
    EXAMPLE: xOffsetAbsoluteRandom: 5

  yOffsetAbsoluteRandom                                 float                1.13     Random offset by +/- this value
    EXAMPLE: yOffsetAbsoluteRandom: 5

  xSpeedRelative                                        float                1.13     Moves the effect sprite on relative horizontal position with specified speed. Negative will move left relative to source, positive will move right relative to source. Useful for thrust effects for moving units
    EXAMPLE: xSpeedRelative: 2

  ySpeedRelative                                        float                1.13     Moves the effect sprite on relative vertical position with specified speed. Negative will move down relative to source, positive will move up relative to source. Useful for thrust effects for moving units
    EXAMPLE: ySpeedRelative: 3

  xSpeedRelativeRandom                                  float                1.13     Randomly change by -value to value
    EXAMPLE: xSpeedRelativeRandom: 2

  ySpeedRelativeRandom                                  float                1.13     Randomly change by -value to value
    EXAMPLE: ySpeedRelativeRandom: 2

  xSpeedAbsolute                                        float                1.13     Moves the effect sprite on absolute horizontal position with specified speed. Negative will move left relative to map, positive will move right relative to map. Useful for smoke effects on structures
    EXAMPLE: xSpeedAbsolute: 5

  ySpeedAbsolute                                        float                1.13     Moves the effect sprite on absolute vertical position with specified speed. Negative will move down relative to map, positive will move up relative to map. Useful for smoke effects on structures
    EXAMPLE: ySpeedAbsolute: 5

  xSpeedAbsoluteRandom                                  float                1.13     Randomly change by -value to value
    EXAMPLE: xSpeedAbsoluteRandom: 2

  ySpeedAbsoluteRandom                                  float                1.13     Randomly change by -value to value
    EXAMPLE: ySpeedAbsoluteRandom: 2

  hOffset                                               float                1.13     height offset from source. May be mistakenly confused with yOffsetAbsolute
    EXAMPLE: hOffset: 4

  hOffsetRandom                                         float                1.13     Randomly change by -value to value
    EXAMPLE: hOffsetRandom: 5

  hSpeed                                                float                1.13     Sets the speed to change the height of the effect
    EXAMPLE: hSpeed: 1

  hSpeedRandom                                          float                1.13     Randomly change by -value to value
    EXAMPLE: hSpeedRandom: 1

  dirOffset                                             float                1.13     Sets the static direction of the effect
    EXAMPLE: dirOffset: 43

  dirOffsetRandom                                       float                1.13     Randomly change by -value to value
    EXAMPLE: dirOffsetRandom: 50

  dirSpeed                                              float                1.13     Sets the rotation speed of the effect
    EXAMPLE: dirSpeed: 2

  dirSpeedRandom                                        float                1.13     Randomly change by -value to value
    EXAMPLE: dirSpeedRandom: 3

  frameIndex                                            int                  1.13     Use a specific frame from strip index
    EXAMPLE: frameIndex: 0

  frameIndexRandom                                      bool -?                       Use random frame from strip index
    EXAMPLE: frameIndexRandom: true

  stripIndex                                            int/string           1.13     A built-in image set to use. Cannot be used with custom image. Options: effects/explode_big/light_50/flame/effects/effects2/projectiles/projectiles2/explode_bits
    EXAMPLE: stripIndex: projectiles

  image                                                 image                1.13     Custom image file to use. Cannot be used with stripIndex.
    EXAMPLE: image: whitePuff.png

  imageShadow                                           image                         Custom image file to use for shadows
    EXAMPLE: imageShadow: AUTO

  scaleTo                                               float                1.13     Defaults to 1. Resizes the unit into the specified scale
    EXAMPLE: scaleTo: 2

  scaleFrom                                             float                1.13     Defaults to 1. Resizes the unit from the specified scale
    EXAMPLE: scaleFrom: 4

  color                                                 color                1.13     Defaults #FFFFFFFF. Changes the color of the effect sprite. Use pure white sprite for most use cases.
    EXAMPLE: color: #ff00ff

  teamColorRatio                                                                      Sets team coloration between 0-1. Team color is dependent from source unit
    EXAMPLE: teamColorRatio: 1

  drawUnderUnits                                        bool                 1.13     Renders the effect under all units
    EXAMPLE: drawUnderUnits: false

  fadeInTime                                            float                1.13     Fade alpha from 0% to 100% for this time at start
    EXAMPLE: fadeInTime: 2s

  fadeOut                                               bool                 1.13     Fade alpha from 100% to 0% based on life. Set alpha is higher than 1 to delay fade
    EXAMPLE: fadeOut: 4s

  alpha                                                                               Capped between 0-1. Can be set higher than 1 to delay fadeOut effects
    EXAMPLE: alpha: 1

  shadow                                                                              True to draw a shadow. Forced true if imageShadow is used
    EXAMPLE: shadow: false

  pivotOffset                                           float                1.15     Similar to dirOffset, this will also rotate relative keys and child elements
    EXAMPLE: pivotOffset: 20

  pivotOffsetRandom                                     float                1.15     Offsets pivot between +/- of specified value
    EXAMPLE: pivotOffsetRandom: 34

  total_frames                                          int                  1.13     Total frames of 'image', used with animation or frameIndex. Only needed with custom images
    EXAMPLE: total_frames: 10

  animateFrameStart                                     int                  1.13     Starting frame for the animation
    EXAMPLE: animateFrameStart: 0

  animateFrameEnd                                       int                  1.13     Ending frame for the animation
    EXAMPLE: animateFrameEnd: 3

  animateFramePingPong                                  int                  1.13     If true, animation will go back and forth within the frames
    EXAMPLE: animateFramePingPong: true

  animateFrameSpeed                                     time                 1.13     Sets the animation speed. The lower the value, the slower it plays
    EXAMPLE: animateFrameSpeed: 10

  animateFrameSpeedRandom                               time                 1.13     Randomizes how fast the effect animation plays
    EXAMPLE: animateFrameSpeedRandom: 20

  animateFrameLooping                                   bool                 1.14     Defaults false. When false effect is removed when animation ends
    EXAMPLE: animateFrameLooping: true


====================================================================================================
SECTION: [animation_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  onActions:                                            enum                 1.13     Automatically plays the animation on specified events. Options: move, attack, idle, underConstruction, underConstructionWithLinkedBuiltTime, queuedUnits, repair
    EXAMPLE: onActions: move

  onActionsQueuedUnitPlayAt:                            float                1.13     For onAction: queuedUnits. Amount queue needs to reach before starting, set between 0-1
    EXAMPLE: onActionsQueuedUnitPlayAt: 0

  blendIn:                                              time                 1.13     Blend with last animation for this time
    EXAMPLE: blendIn: 2s

  blendOut:                                             time                 1.13     Blend with next animation for this time
    EXAMPLE: blendOut: 3s

  pingPong                                              bool                 1.13     Play animation in reverse after it ends
    EXAMPLE: pingPong: true

  KeyframeTimeScale:                                    float                1.13     Scales all keyframe times, useful to make an animation faster/slower without changing everything
    EXAMPLE: KeyframeTimeScale: 1

  arm#_[time]                                                                1.13     Adds a keyframe at time. Use multiple times to create animation.
    EXAMPLE: arm1_5s: {x: 5, dir: 90 }

  leg#_[time]                                                                         Adds a keyframe at time. Use multiple times to create animation.
    EXAMPLE: leg1_3s: {dir: 300}

  body_[time]                                                                         Adds a keyframe at time for body. Only frame and scale allowed on body
    EXAMPLE: body_4s: {frame: 4, scale: 0.5}

  effect_[time]                                                              1.14     Spawn effects while playing an animation
    EXAMPLE: effect_2s: {name:CUSTOM|myExplode,  x: 0,y: 5}

  direction_units                                       float                         Overrides [graphics]animation_direction_units while this animation is playing
    EXAMPLE: direction_units: 45

  direction_strideX:                                    int                           Overrides [graphics]animation_direction_strideX
    EXAMPLE: direction_strideX: 20

  direction_strideY:                                    int                           Overrides [graphics]animation_direction_strideY
    EXAMPLE: direction_strideY: 50

  direction_starting:                                   float                         Overrides [graphics]animation_direction_starting
    EXAMPLE: direction_starting: 0

  start : int                                                                1.13     Start image frame. deprecated

  end : int                                                                  1.13     End image frame. deprecated

  scale_start : float                                                        1.13     Start scale. Deprecated, use body keyframes instead.

  scale_end : float                                                          1.13     End scale. Deprecated, use body keyframes instead.

  speed : float                                                              1.13     Speed, smaller is faster. Only effects start, end, scale_start, scale_end

  neutralTeam                                                                1.13.3   Spawn the unit on the neutral team instead of the same team as source
    EXAMPLE: spawnUnits: tank*3(neutralTeam=true, offsetRandomX=20, offsetRandomY=20, gridAlign=true)

  setToTeamOfLastAttacker                                                    1.13.3   Spawn the unit on the last attacker of source (useful on [core]unitsSpawnedOnDeath)
    EXAMPLE: spawnUnits: egg(setToTeamOfLastAttacker=true)

  spawnChance                                           0-1                  1.13.3   Chance this unit will spawn. Defaults to 1.
    EXAMPLE: spawnUnits: shards(spawnChance=0.3)

  spawnSource                                           unit ref             1.15     Changes spawn location and team of spawned units to this unit ref.
    EXAMPLE: spawnUnits: tank(spawnSource=memory.lastLocation)

  maxSpawnLimit                                                              1.13.3   Useful with spawnChance, max number of units to spawn in total
    EXAMPLE: spawnUnits: treeA(spawnChance=0.5, maxSpawnLimit=1), treeB(maxSpawnLimit=1)

  gridAlign                                                                  1.13.3   Align spawn location to grid, useful for buildings
    EXAMPLE: spawnUnits: hovertank(gridAlign: true)

  skipIfOverlapping                                     bool                 1.13.3   Don't spawn this unit if spawn in an invalid location. Eg on units or over water when LAND based
    EXAMPLE: spawnUnits: crates*10(skipIfOverlapping=true, offsetRandomX=40, offsetRandomY=40, gridAlign=true)

  offsetX                                               float                1.13.3   Sets horizontal position relative to source
    EXAMPLE: spawnUnits: jet(offsetX=20), jet(offsetX=-20)

  offsetY                                               float                1.13.3   Sets vertical position relative to source
    EXAMPLE: spawnUnits: scout(offsetY=40), scout(offsetY=-40)

  offsetDir                                             float                1.13.3   Set the facing direction of spawned units
    EXAMPLE: spawnUnits: bike(offsetDir=-45)

  offsetHeight                                          float                1.13.3   Sets the height for spawned units
    EXAMPLE: spawnUnits: drone(offsetHeight=20)

  offsetRandomX                                         float                1.13.3   Sets random horizontal position relative to source
    EXAMPLE: spawnUnits: k9*5(offsetRandomX=45)

  offsetRandomY                                         float                         Sets random vertical position relative to source
    EXAMPLE: spawnUnits: scanners*10(offsetRandomY=60)

  offsetRandomDir                                       float                         Set random facing direction of spawned units
    EXAMPLE: spawnUnits: chickens*20(offsetRandomDir=360)

  addResources                                          resource ref         1.13.3   Give spawn unit those resources, can be used to set flags that trigger actions
    EXAMPLE: spawnUnits: crates(addResources=gold:30|stone:10, spawnChance=0.5)

  transportedUnitsToTransfer                            int                  1.14     Puts the designated amount of transported units into the transport of the spawned unit.
    EXAMPLE: spawnUnits: transporter(transportedUnitsToTransfer=5)

  copyWaypointsFrom                                     unit ref             1.15     Copies all waypoints on target to created units.
    EXAMPLE: spawnUnits: tank(copyWaypointsFrom=self)

  alwayStartDirAtZero                                   bool                 1.14     Sets the direction of spawned unit to 0 degrees.
    EXAMPLE: spawnUnits: builder(alwayStartDirAtZero=true)

  damagingBorder                                                                      Used with techLevel

  zoneMarker                                                                          Used with techLevel

  techLevel                                             int                           Usually used in BR maps, controls the size of safe zone markers and damaging borders. 1 techLevel = 10x10 tiles = 200 pixels in diameter
    EXAMPLE: spawnUnits: damagingBorder(techLevel=100), zoneMarker(techLevel=40)

  NOTE: If you need to use offsetDir to rotate your projectiles in a different direction (like for shrapnel), you must also set these projectile speeds low enough to force their trajectory                               Example:
    EXAMPLE: [projectile_main]
spawnProjectilesOnExplode: shrapnel(offsetDir=90), shrapnel(offsetDir=-90)
[projectile_shrapnel]
turnSpeed: 0

  recursionLimit                                        int                  1.14     Prevents loops, useful with spawning itself so it doesn't infinitely spawn, good for chain exploding. (Recommended no more than 4 if spawning more than 3 projectiles)
    EXAMPLE: spawnProjectileOnEndOfLife: flamingBits*4(spawnChance=0.35, recursionLimit=2)

  xOffsetRelative                                       float                1.14     Similar to offsetX, but the offset is relative to the position of the projectile
    EXAMPLE: spawnProjectileOnCreate: homingEnergy(xOffsetRelative=5, yOffsetRelative=10)

  yOffsetRelative                                       float                1.14     Similar to xOffsetRelative, but for Y axis
    EXAMPLE: spawnProjectileOnCreate: homingEnergy(xOffsetRelative=5, yOffsetRelative=10)

  offsetRandomXY                                        float                1.14     The offset in both directions to randomly spawn, makes truly random spawning within an area
    EXAMPLE: spawnProjectileOnEndOfLife: shrapnels*20(spawnChance=0.2, offsetRandomXY=100)

  true                                                  bool                          This value will meet the condition
    EXAMPLE: autoTrigger: true

  false                                                 bool                          This value will not meet the condition
    EXAMPLE: autoTrigger: false

  if                                                                                  Start all logic booleans with if, unless just using booleans (true/false)
    EXAMPLE: isActive: if self.hasFlag(id=1)

  and                                                   bool                          Connector. Adds another condition to the list. All conditions linked by this must be reached to return true.
    EXAMPLE: autoTrigger: if self.isInWater and self.energy>=1

  or                                                    bool                          Connector. Adds another condition to the list. Just one of the conditions linked must be reached to return true
    EXAMPLE: autoTrigger: if (self.energy>=2 or self.ammo>=1) and self.isFlying

  not                                                   bool                          Conditional. Reverts meaning of the next logic boolean. Useful to set negative conditions
    EXAMPLE: if not self.isOverLiquid (the opposite of being over liquid)

  <                                                     bool                 1.15     Return true if number on left is smaller. Means less than next value
    EXAMPLE: isLocked: if self.ammo < 1

  >                                                     bool                 1.15     Return true if number on right is smaller. Means greater than next value
    EXAMPLE: autoTrigger: if self.isInWater and self.energy>=1

  <=                                                    bool                 1.15     Return true if number on left is smaller or equal. Means less or equal than next value
    EXAMPLE: requireConditional: if numberOfUnitsInTeam(withTag="fish") >= 10

  >=                                                    bool                 1.15     Return true if number on right is smaller or equal. Means greater or equal than next value
    EXAMPLE: requireConditional: if numberOfUnitsInTeam(withTag="fish") <= 11

  ==                                                    bool                 1.15     Return true if number, unit, string, bool on both sides is the same. Means equal to next value
    EXAMPLE: hidden: if memory.tail == 0

  !=                                                    bool                 1.15     Return true if number, unit, string, bool on both sides different. Means different to next value
    EXAMPLE: isVisible: if memory.message != parent.readUnitMemory(name="stateMessage", type="string")

  +                                                     same type            1.15     Add two numbers or join two strings. Means addition to this value
    EXAMPLE: setUnitMemory: stockpile = self.ammo + customTarget1.resource.reserve

  -                                                     same type            1.15     Subtract two numbers. Means substraction to this value
    EXAMPLE: setUnitMemory: stockpile = self.ammo - customTarget2.resource.reserve

  /                                                     same type            1.15     Divide two numbers. Means division of two values
    EXAMPLE: setUnitMemory: damageMultiplierBuffer = memory.population / memory.death

  *                                                     same type            1.15     Multiply two numbers. Means multiplication of two values
    EXAMPLE: setUnitMemory: reserveCash = memory.population * self.resource.credits

  %                                                     same type            1.15     Divides two numbers and returns the remainder. Means a percentage of a value
    EXAMPLE: setUnitMemory: isEvenX = select(self.x % 2, true, false)

  self.isUnderwater()                                   bool                          Checks if unit is underwater
    EXAMPLE: autoTrigger: if self.isUnderwater()

  self.isAtGroundHeight()                               bool                          Checks if unit is in surface level
    EXAMPLE: autoTrigger: if thisActionTarget.isAtGroundHeight()

  self.isFlying()                                       bool                          Checks if unit is flying
    EXAMPLE: isActive: if self.isFlying()

  self.isMoving()                                       bool                          Checks if unit is moving on its own by any means
    EXAMPLE: isLocked: if self.isMoving()

  self.isReversing()                                    bool                 1.15     Checks if unit is moving backwards
    EXAMPLE: isLockedAlt2: if self.isReversing()

  self.isAtTopSpeed()                                   bool                          Checks if the unit current speed matches the moveSpeed property
    EXAMPLE: autoTrigger: if self.isAtTopSpeed()

  self.isInWater()                                      bool                          Checks if unit touches a water tile
    EXAMPLE: isLocked: if not self.isInWater()

  self.isOverwater()                                    bool                          Checks if unit is touching or over a water tile
    EXAMPLE: isLocked: if not self.isOverwater()

  self.isOverLiquid()                                   bool                          Checks if unit is touching or over a liquid tile (water, lava)
    EXAMPLE: isLocked: if not self.isOverLiquid()

  self.isOverClift()                                    bool                          Checks if unit is touching a cliff tile
    EXAMPLE: isLocked: if not self.isOverClift()

  self.isOverPassableTile()                             bool                          Checks if unit is touching or over a specific tile (parameters: type, Type Values: NONE, LAND, BUILDING, HOVER, OVER_CLIFF, OVER_CLIFF_WATER, AIR, WATER)
    EXAMPLE: isLockedAlt: if self.isOverPassableTile(type="OVER_CLIFF_WATER")

  self.isOverOpenLand()                                 bool                          shortcut for self.isOverPassableTile(type='LAND')
    EXAMPLE: hidden: if not self.isOverOpenLand()

  self.hasResources()                                   bool                 1.13.3   Checks if unit has these resources. Can check multiple resources at the same time (all price parameters).
    EXAMPLE: isActive self.hasResources(credits=1, energy=2)

  self.resource()                                       float / bool                  Checks a single resource (parameters: type, greaterThan, lessThan) (1.15 returns float with no parameters)
    EXAMPLE: isActive: self.resource(type=gold) >= 10

  self.resource.RESOURCE_TYPE                           float                1.15     Shortcut for: self.resource(type='RESOURCE_TYPE')
    EXAMPLE: addResourcesWithLogic: hp += self.resource.gold

  self.isResourceLargerThan                             bool                 1.14?    Compare two resource between each other, note multiplyTargetBy doesn't make any changes. (parameters: source=x, compareTarget=x, byMoreThan=x, multiplyTargetBy=x)
    EXAMPLE: autoTrigger: self.isResourceLargerThan(source=oil, compareTarget=sauce, byMoreThan=100, multiplyTargetBy=0.2)

  self.hp()                                             float / bool                  Returns the current health of the unit (parameters: greaterThan, lessThan, empty, full). (1.15 returns float with no parameters)
    EXAMPLE: addResourcesWithLogic: energy += select(self.hp > self.energy, 10, 3)

  self.maxHp()                                          float / bool         1.15     Returns the maximum health of the unit (parameters: greaterThan, lessThan, empty, full). (1.15 returns float with no parameters)
    EXAMPLE: setResourcesWithLogic: sauce = self.maxHp * self.energy

  self.height()                                         float / bool                  Returns the height of the unit from the ground (parameters: greaterThan, lessThan, empty, full)  (1.15 returns float with no parameters)
    EXAMPLE: imageScale: 1 + (self.height * 0.1)

  self.ammo()                                           int / bool                    Returns int if no parameters, boolean with any parameters (parameters: greaterThan, lessThan, empty, full)
    EXAMPLE: isLocked: if self.ammo < 1

  self.isAmmoEmpty()                                    bool                          shortcut for self.ammo(empty=true)
    EXAMPLE: isLockedAlt: self.isAmmoEmpty()

  self.ammoIncludingQueued()                            int / bool                    Also includes ammo from actions still in queue (parameters: greaterThan, lessThan, empty, full)
    EXAMPLE: isLocked: if self.ammoIncludingQueued(lessThan=12)

  self.energy()                                         float / bool                  (parameters: greaterThan, lessThan, empty, full)   (1.15 returns float with no parameters)
    EXAMPLE: isVisible: if self.energy() > 50

  self.energyIncludingQueued()                          float / bool                  Also includes energy from actions still in queue (parameters: greaterThan, lessThan, empty, full)
    EXAMPLE: isVisible: if self.energyIncludingQueued()

  self.isEnergyFull()                                   bool                          shortcut for self.energy(full=true)
    EXAMPLE: isLocked: if self.isEnergyFull()

  self.isEnergyEmpty()                                  bool                          shortcut for self.energy(empty=true)
    EXAMPLE: isActive: if self.isEnergyEmpty()

  self.maxEnergy()                                      float / bool         1.15p10  Returns maximum energy (defaults to energyMax value in core, dynamically adjusts to changed value made by setUnitStats)
    EXAMPLE: isLocked: if memory.kills < self.maxEnergy()

  self.isEnergyRecharging()                             bool                          Checks if unit energy is passively recharging
    EXAMPLE: text: Reload %{select(self.isEnergyRecharging(), "", "[ready")}

  self.shield()                                         float / bool                  (parameters: greaterThan, lessThan, empty, full)   (1.15 returns float with no parameters)
    EXAMPLE: isActive: if self.shield() == 0

  self.maxShield()                                      float / bool         1.15p10  Returns the maximum shield value (defaults to maxShield value in core, but dynamically adjusts to changes made with setUnitStats)
    EXAMPLE: isLockedAlt2: if self.energy <= self.maxShield()/2

  self.kills()                                          int / bool                    Returns the number of kills the unit has done (parameters: greaterThan, lessThan)   (1.15 returns int with no parameters)
    EXAMPLE: setUnitMemory: score = self.kills()*3

  self.queueSize()                                      int / bool           1.13.3   Checks the amount of queued build actions and other actions in the invoked unit(parameters: greaterThan, lessThan, full, empty, equalTo)  (1.15 returns float with no parameters) 1.15p9 adds a new parameter: withActionTag="#"
    EXAMPLE: isLocked: if self.queueSize() >= 4

  self.teamId()                                         int                  1.15     Return team id of unit or marker. Starts at 0. (but -1 for a neutral team)
    EXAMPLE: switchToteam: lastDamagedBy.teamId()

  self.teamName()                                       string                        Returns allied group team name
    EXAMPLE: showMessageToAllPlayers: %{attacking.teamName()} dominated %{thisActionTarget.teamName()}

  self.playerName()                                     string                        Returns player's name
    EXAMPLE: showQuickWarLogToAllPlayers: %{self.playerName()} destroyed %{thisActionTarget.playerName()}'s %{thisActionTarget}

  self.x()                                              float                         Returns the horizontal position of the unit
    EXAMPLE: description: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{self.dir}

  self.y()                                              float                         Returns the vertical position of the unit
    EXAMPLE: description: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{self.dir}

  self.z()                                              float                         Returns the elevation position of the unit (identical to self.height)
    EXAMPLE: description: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{self.dir}

  self.dir()                                            float                         Returns the unit's direction
    EXAMPLE: description: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{self.dir}

  self.priceCredits()                                                                 Returns the price of the unit. Only returns the credit price. Does not support custom resources.
    EXAMPLE: addResourcesWithLogic: credits = attacking.priceCredits

  self.builtAmount()                                    float                1.15p9   Returns the current value of build progression of the unit
    EXAMPLE: stack_indexCount: int(12 * (self.builtAmount * 0.1))

  self.completed()                                      bool                 1.15p9   Shortcut for self.builtAmount() == 1
    EXAMPLE: autoTrigger: if self.completed()

  self.speed()                                          float                1.15p9   Returns current speed of the unit
    EXAMPLE: isLockedAlt: if self.speed < 2

  self.maxMoveSpeed()                                   float                1.15p9   Returns the maximum possible speed set for the unit (value set for moveSpeed property, may be manipulated with setUnitStats)
    EXAMPLE: autoTrigger: if memory.defaultSpeed < self.maxMoveSpeed

  self.hasFlag()                                        bool                 1.13     Boolean flag saved into units for mods to use. (parameters: id=0-31)
    EXAMPLE: autoTrigger: if self.hasFlag(id=1)

  self.tags()                                           bool                 1.13     Checks or return tags of a unit (parameters: includes)
    EXAMPLE: autoTrigger: if self.tags(includes='fish')

  self.globalTeamTags()                                 bool                 1.13.3   Checks or returns global tags on a unit (parameters: includes)
    EXAMPLE: autoTrigger: if self.globalTeamTags(includes='animal')

  self.transportingCount()                              int                  1.13     (parameters: greaterThan, lessThan, empty) (1.15 returns int with no parameters)
    EXAMPLE: text: Load - %{self.transportingCount()}

  self.numberOfAttachedUnits()                          int / bool           1.13     (withTag,  greaterThan, lessThan) (1.15 returns int with no parameters)
    EXAMPLE: text: Cores - %{self.numberOfAttachedUnits()}

  self.isAttacking()                                    bool                 1.13     Checks if the unit is attacking something
    EXAMPLE: setUnitStats: moveSpeed=select(self.isAttacking, 4, 2)

  self.hasActiveWaypoint()                              bool                 1.13     (parameters: type=WAYPOINT_TYPE)
    EXAMPLE: text: Waypoint - %{self.hasActiveWaypoint(type='move')}

  self.numberOfQueuedWaypoints()                        int / bool           1.15     (parameters: type=WAYPOINT_TYPE, amount=#)
    EXAMPLE: text: Waypoint Queue - %{self.numberOfQueuedWaypoints(type='attackMove') >= 2}

  self.transportingUnitWithTags()                       bool                 1.13     (parameters: includes)
    EXAMPLE: requireConditional: if self.transportingUnitWithTags(includes='human')

  self.hasParent()                                      bool                 1.13     Checks if a unit is attached or a passenger of a unit. For both attachments and transports (parameters: [withTag=x] )
    EXAMPLE: isVisible: if self.hasParent

  self.hasTakenDamage()                                 float / bool         1.13     Checks when the unit last took damage (parameters: withinSeconds=X, laterThanSeconds=X)  (v1.15 returns float in seconds with no parameters)
    EXAMPLE: isActive: if self.hasTakenDamage(withinSeconds=1)

  self.timeAlive()                                      float / bool         1.13     Checks how long the unit is alive (parameters: withinSeconds=X, laterThanSeconds=X)  (v1.15 returns float in seconds with no parameters)
    EXAMPLE: isLocked: if self.timeAlive() > 100

  self.lastConverted()                                  float / bool         1.13     Check the time the unit last converted from (parameters: withinSeconds=X, laterThanSeconds=X)  (v1.15 returns float in seconds with no parameters)
    EXAMPLE: isLockedAlt2: if self.lastConverted()

  self.customTimer()                                    float / bool         1.13     Returns the current value in the unit's custom timer (parameters: withinSeconds=X, laterThanSeconds=X)  (v1.15 returns float in seconds with no parameters)
    EXAMPLE: autoTrigger: if self.customTimer() > 120

  self.isOnNeutralTeam()                                bool                 1.13     Checks if the unit belongs to the neutral team (Team ID -1)

  self.numberOfUnitsInTeam()                            int / bool           1.13     Checks amount of player units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: isLocked: if numberOfUnitsInTeam(withTag='techUnlockBuilding', lessThan=5)

  self.numberOfUnitsInAllyTeam()                        int / bool           1.13     Checks amount of allied and player units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: description: TEAM %{self.teamName}\n\nTank Population: %{self.numberOfUnitsInAllyTeam()}

  self.numberOfUnitsInAllyNotOwnTeam()                  int / bool           1.14     Checks amount of allied only units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: isActive: if self.numberOfUnitsInAllyNotOwnTeam() > 100

  self.numberOfUnitsInEnemyTeam()                       int / bool           1.13.3   Checks amount of enemy units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: autoTrigger: if self.numberOfUnitsInEnemyTeam(withinRange=500) > 0

  numberOfUnitsInNeutralTeam()                          int / bool           1.14     Checks amount of neutral units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: isVisible: if memory.deadzone.numberOfUnitsInNeutralTeam() >= 10

  numberOfUnitsInAggressiveTeam()                       int / bool           1.14     Checks amount of aggressive neutral units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: isVisible: if memory.deadzone.numberOfUnitsInAggressiveTeam() >= 30

  numberOfUnitsInAllTeams()                             int / bool           1.15p9   Checks amount of any units that meet the parameters (parameters: withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue) (v1.15 returns int with no parameters)
    EXAMPLE: text: Birds: %{numberOfUnitsInAllTeams(withTag="bird")}

  self.hasUnitInTeam()                                  bool                 1.13.3   (parameters: withTag, withinRange, incompleteBuildings, factoryQueue) alias for numberOfUnitsInTeam
    EXAMPLE: isLocked: if self.hasUnitInTeam(withTag="fish")

  self.noUnitInTeam()                                   bool                          (parameters: withTag, withinRange, incompleteBuildings, factoryQueue) alias for numberOfUnitsInTeam
    EXAMPLE: isLocked: if self.noUnitInTeam(withTag="infected")

  self.isControlledByAI()                               bool                          Checks if a unit is controlled by AI.
    EXAMPLE: text: Robot? %{select(self.isControlledByAI(), "Yes", "No")}

  self.readUnitMemory()                                 any type             1.15     (name:string, type:string{boolean,unit,float,string}, [default])
    EXAMPLE: isActive: if parent.readUnitMemory('boostTarget', type='unit') == self

  thisActionTarget                                      unit / marker        1.15     Current target or location targeted.
    EXAMPLE: fireTurretXAtGround: mainGun (thisActionTarget==Marker with ground location)
alsoTriggerAction: x (thisActionTarget==Same as original action)
[turret]onShoot_triggerActions: x  (thisActionTarget==Target that was shot at)
takeResources_triggerActionIfAnyCollected: x (thisActionTarget==Target with resources)
addWaypoint_triggerActionIfMatched: x (thisActionTarget == Marker for move/Target for attack, etc. Note: use addWaypoint_maxTime:0 if you want to search only)

  thisActionIndex                                       int                  1.15     Used with alsoTriggerActionRepeat and takeResources_triggerActionForEach
    EXAMPLE: alsoTriggerActionRepeat: thisActionIndex

  eventSource                                           unit / marker        1.15     Current trigger from an autoTriggerOnEvent, otherwise null
    EXAMPLE: autoTriggerOnEvent: tookDamage (eventSource==Unit that caused damage)
autoTriggerOnEvent: killedAnyUnit (eventSource==Unit that was killed)
autoTriggerOnEvent: transportingNewUnit (eventSource==Unit that was transported)
autoTriggerOnEvent: transportUnloadedOrRemovedUnit (eventSource==Unit unloaded)
autoTriggerOnEvent: queuedUnitFinished (eventSource==New unit made)
autoTriggerOnEvent: touchTargetSuccess (eventSource==Target touched)

  attachment                                            unit                 1.15     Returns unit attachment as reference (parameters: [slot], [withTag])
    EXAMPLE: setCustomTarget2: self.attachment(withTag='x').lastDamagedBy.getAsMarker()

  transporting                                          unit                 1.15     Returns unit passenger as reference (parameters: [slot])
    EXAMPLE: autoTrigger: if self.transporting(slot=0).hasResources(gold=100)

  attacking                                             unit                 1.15     Current target this is attacking, might not be the current waypoint target.
    EXAMPLE: isLocked: if attacking.tags(includes='bug') and attacking.hp < 20

  lastDamagedBy                                         unit                 1.15     Last unit that attacked this.
    EXAMPLE: teleportTo: lastDamagedBy

  parent                                                unit                 1.15     The transporter or attachment parent. (Note: units are suspended state when transported without attachment slot)
    EXAMPLE: autoTrigger: if parent.energy > 100

  activeWaypointTarget                                  unit                 1.15     Current active waypoint target. Includes attacking, transporting, repairing, etc.
    EXAMPLE: isActive: if distanceBetween(self, activeWaypointTarget) < 100

  customTarget1                                         unit                 1.15     Custom memory, defaults to the unit that created this unit.
    EXAMPLE: isLocked: if parent.customTarget1 == self

  customTarget2                                         unit                 1.15     Custom memory, defaults to null
    EXAMPLE: fireTurretXAtGround_withPosition: customTarget2

  nearestUnit                                           unit                 1.15     (withinRange=500, withTag='x', relation='any') Search for a unit (not recommended in autoTrigger check for perfomance)
    EXAMPLE: setCustomTarget1: nearestUnit(withTag="derrick")

  globalSearchForFirstUnit                              unit                 1.15     (withTag=x, relation) - Returns first (and oldest) unit found matching the filter. Slow, avoid using in autoTrigger checks
    EXAMPLE: self.globalSearchForFirstUnit(withTag='gameController', relation='neutral')

  nullUnit                                              unit                 1.15     returns a null unit reference, useful for comparisons
    EXAMPLE: isLockedAlt2: if self.parent == nullUnit

  getAsMarker                                           marker               1.15     creates a temporary marker at the position a unit is right now. Markers are very fast to create and automatically removed when no longer needed. Is not linked to any unit and still exists when the unit dies, and stays the same when source moves.
    EXAMPLE: lastDamagedBy.getAsMarker()

  getOffsetAbsolute                                     marker               1.15     ([x],[y],[height]) Returns marker with absolute offset (-y is north, +x is east)
    EXAMPLE: addWaypoint_target_fromReference: unitref getOffsetAbsolute(self.x, self.y+1000, self.z)

  getOffsetRelative                                     marker               1.15     ([x],[y],[height],[dirOffset]) Returns marker with relative offset. (y+ is forwards)
    EXAMPLE: self.getOffsetRelative(y=100).nearestUnit(withinRange=70, withTag='mouse') != null

  readUnitMemory                                        all arguments        1.15     Reads memory from the unit reference  (e.g. self.readUnitMemory, parent.readUnitMemory, attacking.readUnitMemory)
    EXAMPLE: self.readUnitMemory('ammoType', type='string'), parent.readUnitMemory(''attachmentArray", type="unit[ ]", index=5), attacking.readUnitMemory("wishlist", type="string[ ]")[12]

  memory.NAME                                           self only            1.15     Shortcut to read the self with current defineUnitMemory types. Cannot be used on other units, use readUnitMemory for that. Don't call with self.
    EXAMPLE: autoTrigger: if memory.experience > 100

  memory.NAME.size                                      int                  1.15     Returns the size of the array (including empty indexes between indexes)
    EXAMPLE: setUnitMemory: squadSize = memory.squad.size

  memory.NAME.contains(query)                           bool                 1.15     Returns true if the query is found within the array. Basically searches something if it exists.
    EXAMPLE: autoTrigger: if memory.savedCoord.contains(lastDamagedBy.customTarget1.customTarget1)

  distance(x1, y1, x2, y2)                              float                1.15     Returns the difference between two points
    EXAMPLE: isLocked: if distance(self.x, self.y, thisActionTarget.x, thisActionTarget.y) > 350

  distanceSquared(x1, y1, x2, y2)                       float                1.15     Returns the squared difference between two points. Bit faster than distance.
    EXAMPLE: isLocked: if distanceSquared(self.x, self.y, thisActionTarget.x, thisActionTarget.y) > 19

  distanceBetween(unit1, unit2)                         float                1.15     Returns the distance between two units or markers
    EXAMPLE: isLocked: if distanceBetween(self, memory.antenna) > 1000

  distanceBetweenSquared(unit1, unit2)                  float                1.15     Returns the squared distance between two units. Bit faster than distanceBetween
    EXAMPLE: isLocked: if distanceBetweenSquared(self, memory.antenna) > 100

  game.nukesEnabled()                                   bool                 1.15     Returns true if nukes are enabled in this game's settings.
    EXAMPLE: isActive: if not game.nukesEnabled()

  int(x)                                                int                  1.15     Removes decimal places from a number.
    EXAMPLE: stack_indexCount: int(12 * (self.builtAmount * 0.1))

  select(bool, textA, textB)                                                 1.15     returns textA if bool is true otherwise returns textB
    EXAMPLE: setUnitMemory: isEvenX = select(self.x % 2, true, false)

  debug(logicBoolean)                                   string               1.15     Returns a text string helping to explain the reason for the current result. Can see into nested logic, comparisons, and operators.
    EXAMPLE: description: [DEBUG] %{debug(self.x > self.y)}

  str(x)                                                string               1.15     Convert a number, unit or boolean into a string
    EXAMPLE: str(self.energy)+'x' == '100x'

  substring(text,start,end)                             string               1.15     Takes a part of the string from the indicated start and end parts
    EXAMPLE: substring('hello',0,2) == 'he'

  length(string)                                        int                  1.15     Returns string length as number
    EXAMPLE: length(memory.sms)

  squareRoot(num)                                       float                1.15     Requires square root of a number
    EXAMPLE: squareRoot(self.x + self.y)

  min(num1, num2)                                       float                1.15     Returns the smallest number
    EXAMPLE: damage = min(self.hp, self.energy)

  max(num1, num2)                                       float                1.15     Returns the biggest number
    EXAMPLE: max(5, 10) == 10

  createMarker(x, y, [height], [teamId], [dir])         marker               1.15     Creates a marker that can be used to place coordinates for various actions
    EXAMPLE: fireTurretXAtGround_withTarget: createMarker(self.x + 500, self.y + 500)

  eventData(name, type, [default])                      any type             1.15     Displays the data from a specified message from some unit. Only use in autoTriggerOnEvent:newMessage events
    EXAMPLE: text: %{eventData(name="msg", type="string")}

  sin(angle)                                            float                1.15     Takes the sine value of a specified number
    EXAMPLE: sin(10) = 0.173

  cos(angle)                                            float                1.15     Takes the cosine value of a specified number
    EXAMPLE: cos(60) = 0.5

  abs(number)                                           float                1.16     Returns absolute value regardless of negative or positive, needs testing

  coalesce(A,B,[C])                                     float                1.16     Returns first non-null element in the array series

  vec2(x,y)                                             float                1.16     Returns a vec2 coordinates

  vec3(x,y,z)                                           float                1.16     Returns a vec3 coordinates

  rnd(min, max)                                         float                1.15     Produces a random number between minimum and maximum input.
    EXAMPLE: rnd(666, 888)

  lowercase(string)                                     string               1.15     Sets all letters from a string into lowercase
    EXAMPLE: lowercase("Spaghetti Macaroni Lasagna") -> "spaghetti macaroni lasagna"

  uppercase(string)                                     string               1.15     Sets all letters from a string into uppercase
    EXAMPLE: uppercase("fish dish wish") -> "FISH DISH WISH"

  direction(x1, y1, x2, y2)                             float                1.15     Returns the relative angle of x1/y1 and x2/y2
    EXAMPLE: direction(self.x, self.y, attacking.x, attacking.y)

  directionBetween(unit1, unit2)                        float                1.15     Returns the relative angle of unit/marker A and unit/marker B
    EXAMPLE: directionBetween(self, lastDamagedBy)

  self.isInMap()                                        bool                 1.15     Returns true if unit (or marker) is with the game area; Returns false if outside bounds
    EXAMPLE: autoTrigger: if not self.isInMap()

  game.mapWidth()                                       float                1.15     Returns the width of the map
    EXAMPLE: fireTurretXAtGround_withTarget: createMarker(rnd(0, game.mapWidth), rnd(0, game.mapHeight))

  game.mapHeight()                                      float                1.15     Returns the height of the map
    EXAMPLE: fireTurretXAtGround_withTarget: createMarker(rnd(0, game.mapWidth), rnd(0, game.mapHeight))

  self.teamDefeatedTech()                               bool                 1.15p9   Returns true if team is defeated
    EXAMPLE: autoTrigger: if attacking.teamDefeatedTech()

  self.teamWipedOut()                                   bool                          Returns true if team is defeated and wiped out
    EXAMPLE: autoTrigger: if self.teamWipedOut()

  self.teamVictory()                                    bool                          Returns true if team is victorious
    EXAMPLE: autoTrigger: if self.teamVictory()

  anyRuleInGroup:                                       string               1.14     (Only require 1 of the rules in this group pass, instead of all. Use the same group name on other placement rules to create a group.)
    EXAMPLE: anyRuleInGroup: struct

  searchTags:                                           tag list             1.14     Search for any unit with at least one of these tags
    EXAMPLE: searchTags: factory

  searchTeam:                                           relation             1.14     Teams to include in search, can be: own|neutral|allyNotOwn|ally|enemy|any
    EXAMPLE: searchTeam: own

  searchOffsetX:                                        float                1.14     defaults to 0
    EXAMPLE: searchOffsetX: 100

  searchOffsetY:                                        float                1.14     defaults to 0
    EXAMPLE: searchOffsetY: 200

  searchDistance:                                       float                1.14     Required
    EXAMPLE: searchDistance: 500

  excludeIncompleteBuildings:                           bool                 1.14     defaults to false. Might want to set to true depending on the requirement reason
    EXAMPLE: excludeIncompleteBuildings: true

  excludeNonBuildings:                                  bool                 1.14     defaults to false
    EXAMPLE: excludeNonBuildings:true

  minCount:                                             int                  1.14     Set min amount of units that need to be found in search. (eg needs to be near something). Defaults to 0
    EXAMPLE: minCount: 0

  maxCount:                                             int                  1.14     Set max amount of units before match fails (eg cannot be close to something). Defaults to unlimited
    EXAMPLE: maxCount: 1

  blocksPlacement:                                      bool                 1.14     Defaults to true.
    EXAMPLE: blocksPlacement: false

  cannotPlaceMessage:                                   LocaleString         1.14     Highly Recommended. Message shown to player if this rule fails (will be first failing rule if using anyRuleInGroup).
    EXAMPLE: cannotPlaceMessage: "No factory is nearby"

  checkEachTile:                                        bool                 1.14     defaults to true (set to false to only test unit center, true checks each tile under the unit which shows up on the placement grid. Can be easier to see requirements with true)
    EXAMPLE: checkEachTile: false

  credits                                                                             Global resource
    EXAMPLE: price: 100

  energy                                                                              Energy used for laser shield and ammunition
    EXAMPLE: resourceUsage: energy=1

  hp                                                                                  Unit hitpoints
    EXAMPLE: addResources: hp += 100

  shield                                                                              Shielding for units
    EXAMPLE: price: hp=-100, shield=100

  ammo                                                                                Hidden value on each unit for use by mods
    EXAMPLE: price: hp=-100, shield=101

  setFlag                                                                             use with addResources, resourceUsage or price. 0-31. Flags are stored in each unit
    EXAMPLE: addResources: setFlag=1

  unsetFlag                                                                           use with addResources, resourceUsage or price. 0-31

  hasFlag                                                                             use with price or resourceUsage

  hasMissingFlag                                                                      use with price or resourceUsage

  X                                                                          1.13.3   Any resource defined in [global_resource_x] or [resource_x] sections
    EXAMPLE: gold=5, stone=21, credits=2001


====================================================================================================
SECTION: [global_resource_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  displayName                                           LocaleString         1.13.3   Name of this resource in UI
    EXAMPLE: displayName: Crude Oil

  displayNameShort                                      LocaleString         1.13.3   Resource name on smaller UI elements like action hovertext (Defaults to displayName)
    EXAMPLE: displayNameShort: Oil

  hidden                                                bool                 1.13.3   Hide this resource from the player
    EXAMPLE: hidden: false

  priority                                              float                1.13.3   If 2 or mods/units define a resource with the same NAME, the displayName/displayColor with the highest priority is used
    EXAMPLE: priority: 0.5

  displayColor                                          color                1.13.3   Color, can be hex with optional alpha
    EXAMPLE: displayColor: #FF0000

  displayRoundedDown                                    bool                 1.14     Don't show decimal places to the player
    EXAMPLE: displayRoundedDown

  displayTextPrefix                                     string               1.15     Adds a string before the resource value
    EXAMPLE: displayTextPrefix: -

  displayTextPostfix                                    string               1.15     Adds a string after the resource value
    EXAMPLE: displayTextPostfix: x

  displayTextAppendResource                             resource             1.15     Appends another resource after this resource. It's recommended to give a "hidden" key on the appended resource to hide duplicate.
    EXAMPLE: displayTextAppendResource: sauce

  displayWhenZero                                       bool                 1.15     Displays the resource even when empty. Set as false by default
    EXAMPLE: displayWhenZero: true

  displayPos                                            int                  1.15     Sets the position of the globa resource in the screen
    EXAMPLE: displayPos: 1

  iconImage                                             image                1.15     Shows a custom icon for the specific resource in the HUD and in text
    EXAMPLE: iconImage: icon_oil.png

  iconImageUseInText                                    bool                 1.15     Default as true; Shows resource icon in action description
    EXAMPLE: iconImageUseInText: false

  displayNameHideWhenIconShownInText                    bool                 1.15     Default as false
    EXAMPLE: displayNameHideWhenIconShownInText: true

  displayNameHideWhenIconShownInHUD                     bool                 1.15     Hides the icon in menus referring to resource when true. Default as false
    EXAMPLE: displayNameHideWhenIconShownInHUD: true

  displayColorUseInText                                 bool                 1.15     Shows color in action description
    EXAMPLE: displayColorUseInText: false

  displayInHud                                          bool                 1.15     For resources used in appendResourceInHUD that shouldn't be hidden: true. Defaults true.
    EXAMPLE: displayInHud: false

  appendResourceInHUD                                   resource             1.15     stacks another resource specified after this resource on the HUD.
    EXAMPLE: appendResourceInHUD: sauce

  appendResourceInHUD_whenThisZero:                     bool                 1.15     Defaults as true. When set as false, allows appended resource to be hidden with the parent resource.
    EXAMPLE: appendResourceInHUD_whenThisZero: true

  displayPrefixInHUD                                    string               1.15     Displays text to show before resource value - replaces resource name and removes the colon separator
    EXAMPLE: displayPrefixInHUD: -

  displayPostfixInHUD                                   string               1.15     Similar to the prefix counterpart, but is displayed after the resource value
    EXAMPLE: displayPostfixInHUD: x

  valueInStats:                                         float                1.15     Affects post game stats and replay leaderboard. Defaults to 1.
    EXAMPLE: valueInStats: 0.5

  displayTextAppendResourceWithGap:                     bool                 1.15     Adds a space between this and the appended resource. Defaults as false.
    EXAMPLE: displayTextAppendResourceWithGap: true

  displayDigitGrouping                                  enum                 1.15p9   Sets the symbol for separating place units in: none, comma, space
    EXAMPLE: displayDigitGrouping: comma


====================================================================================================
SECTION: [resource_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  displayName                                           LocaleString         1.13.3   Name of this resource in UI (eg hovering over unit info)
    EXAMPLE: displayName: Sauce

  displayNameShort                                      LocaleString         1.13.3   Shorter version of resource name used on some text ui
    EXAMPLE: displayNameShort: Sus

  hidden                                                bool                 1.13.3   Hide this resource from the player
    EXAMPLE: hidden: true

  equivalentGlobalResourceForAI                         resource                      Used to hint to the AI that a resource node with a local resources could be used to get a different global resource. Eg when a harvester unloads the resource
    EXAMPLE: equivalentGlobalResourceForAI: universal_sauce

  displayRoundedDown                                    bool                 1.14     Rounds off resource values with decimals
    EXAMPLE: displayRoundedDown: true

  # Graphical #                                                              1.15p9   

  image                                                 image                1.15p9   Takes a single image for the decal - NOT TO BE CONFUSED WITH IMAGE STACK
    EXAMPLE: image: fish.png

  layer                                                 enum                 1.15p9   Sets the layer of the decal, values from lowest to highest - shadow,  beforeBody, afterBody, onTop, beforeUI, inactive
    EXAMPLE: layer: beforeUI

  order                                                 float                1.15p9   Defaults as 0, sets a more discrete layering if 2 or more decals takes same layer type. Otherwise order in INI file will be used.
    EXAMPLE: order: 2

  teamColors                                            bool                 1.15p9   When true, automatically reassigns team color to respective teams
    EXAMPLE: teamColors: true

  alpha                                                 logicNumber          1.15p9   0-1, sets transparency of decal (images or line)
    EXAMPLE: alpha: 0.5

  isVisible                                             LogicBoolean         1.15p9   Dynamic Value, draws the decal on a specific condition
    EXAMPLE: isVisible: if self.hp > self.maxHp/2

  # Player Conditions #                                                      1.15p9   

  onlyWhenSelectedByOwnPlayer                           bool                 1.15p9   Defaults as false, displays the decal only if the owner player clicks on the unit
    EXAMPLE: onlyWhenSelectedByOwnPlayer : true

  onlyWhenSelectedByEnemyPlayer                         bool                 1.15p9   Defaults as false, displays the decal only if the enemy player clicks on the unit
    EXAMPLE: onlyWhenSelectedByEnemyPlayer: true

  onlyWhenSelectedByAllyNotOwnPlayer                    bool                 1.15p9   Defaults as false, displays the decal only if the allied player clicks on the unit
    EXAMPLE: onlyWhenSelectedByAllyNotOwnPlayer: true

  onlyWhenSelectedByAnyPlayer                           bool                 1.15p9   Defaults as false, displays the decal only if any player clicks on the unit
    EXAMPLE: onlyWhenSelectedByAnyPlayer: true

  includeParentsSelection                               bool                 1.15p9   Useable with onlyWhenSelectedBy[insertplayer], will also checks parent's selection when true. Useful with attachments.
    EXAMPLE: includeParentsSelection: true

  # "Only if" Conditions #                                                   1.15p9   

  onlyTeam                                              TeamRelation         1.15p9   Draws the decal if value matches the player from the following: own, notOwn, ally, allyNotOwn,  enemy, any
    EXAMPLE: onlyTeam: any

  onlyWhileActive                                       bool                 1.15p9   Draws the decal only if the unit is fully built
    EXAMPLE: onlyWhileActive: true

  onlyWhileAlive                                        bool                 1.15p9   Draws the decal only if the unit is alive (If beforeUI layer default true, else default false)
    EXAMPLE: onlyWhileAlive: false

  onlyInPreview                                         bool                 1.15p9   Only show in sidebar, and building placement preview
    EXAMPLE: onlyInPreview: false

  onlyWithZoomLevelOrMore                               float                1.15p9   Draws the decal only if the zoom level matches or exceeds it, very useful for making 3D units more optimized.
    EXAMPLE: onlyWithZoomLevelOrMore: 0.4

  onlyOnNonPreview                                      bool                 1.15p9   Draws the decal only on the unit itself, not on the preview from building or in sidebar interface
    EXAMPLE: onlyOnNonPreview: true

  onlyPlayersWithUnitControl                            bool                 1.15p9   Draws the decal if a player that selects it has control (that includes shared units in multiplayer)
    EXAMPLE: onlyPlayersWithUnitControl: true

  onlyOnBodyFrameOf                                     int                  1.15p9   Only draw decal when body frame is equal to this
    EXAMPLE: onlyOnBodyFrameOf: 0

  onlyInBlueprint                                       bool                 1.16     Untested prototype

  onlyOnNonBlueprint                                    bool                 1.16     Untested prototype

  # Offsets (Positioning) #                                                  1.15p9   

  xOffsetRelative                                       int                  1.15p9   Sets horizontal offset relative to the unit
    EXAMPLE: xOffsetRelative: 50

  yOffsetRelative                                       int                  1.15p9   Sets vertical offset relative to the unit
    EXAMPLE: yOffsetRelative: 50

  xOffsetAbsolute                                       logicNumber          1.15p9   Dynamic value
    EXAMPLE: xOffsetAbsolute: 40

  yOffsetAbsolute                                       logicNumber          1.15p9   Dynamic value
    EXAMPLE: yOffsetAbsolute: 40

  hOffset                                               int                  1.15p9   Sets the height of the whole decal
    EXAMPLE: hOffset: 10

  dirOffset                                             int                  1.15p9   Sets the direction of the whole decal.
    EXAMPLE: dirOffset: 45

  pivotOffset                                           int                  1.15p9   only affects relative offsets without rotating image
    EXAMPLE: pivotOffset: 45

  alwaysStartDirAtZero                                  bool                 1.15p9   Keeps the decal on north direction regardless of the unit's direction value,  useful for custom in-unit interfaces
    EXAMPLE: alwaysStartDirAtZero: true

  # Image specifics / Stacks #                                               1.15p9   

  imageStack                                            images               1.15p9   Takes one or more image with occassional multipliers for stacking. Also useful for using image stack from a MagicaVoxel slice export
    EXAMPLE: Multiple image file stack: imageStack - carFloor.png, carBody.png*3, carRoof.png*2, antenna.png*10

  imageScale                                            logicNumber          1.15p9   Dynamic value,  scales the decal
    EXAMPLE: imageScale: 1 + (self.height * 0.1)

  imageScaleX                                           logicNumber          1.15p9   Dynamic value
    EXAMPLE: imageScaleX: 1

  imageScaleY                                           logicNumber          1.15p9   Dynamic value
    EXAMPLE: imageScaleY:  (self.hp/self.maxHp)*100

  stack_hOffset                                         float                1.15p9   Sets the height of every layer in a stack. 1 layer = 1 pixel. Can take negative values.
    EXAMPLE: stack_hOffset: 4

  stack_frameOffset                                     int                  1.15p9   Useful for making 3D units, frame to offset by in imageStack. Often a value of 1 is useful in a sprite sheet.
    EXAMPLE: stack_frameOffset: 1

  stack_drawInReverseOrder                              bool                 1.15p9   Renders the stack in the opposite way - last frame goes on bottom, first frame goes to top
    EXAMPLE: stack_drawInReverseOrder: true

  stack_indexStart                                      logicNumber          1.15p9   Offset to start drawing images in the image stack
    EXAMPLE: stack_indexStart: 0

  stack_indexCount                                      logicNumber          1.15p9   Number of images in the image stack to draw.
    EXAMPLE: stack_indexCount: 10

  total_frames                                          int                  1.15p9   Sets the number of frames in an image from an imageStack that uses a spritesheet.
    EXAMPLE: total_frames: 10

  frame_width                                           int                  1.15p9   Sets the frame width in the decal stack
    EXAMPLE: frame_width: 20

  frame_height                                          int                  1.15p9   Sets the frame height in the decal stack
    EXAMPLE: frame_height: 40

  frame                                                 logicNumber          1.15p9   Dynamic value, useful for animations
    EXAMPLE: frame: memory.frame

  addBodyFrameMultipliedBy                              int                  1.15p9   Add body frame number to this decal when set to 1.
    EXAMPLE: addBodyFrameMultipliedBy: 2

  # Shadows #                                                                1.15p9   

  image_shadow                                          image                1.15p9   Sets shadow for the decal. Note that AUTO does not work in this key.
    EXAMPLE: image_shadow: shadow.png

  shadowOffsetX                                         int                  1.15p9   Sets the horizontal position of the decal's shadow
    EXAMPLE: shadowOffsetX: 0

  shadowOffsetY                                         int                  1.15p9   Sets the vertical position of the decal's shadow
    EXAMPLE: shadowOffsetY: 0

  # Marker Positioning #                                                     1.15p9   

  basePosition                                          marker               1.15p9   Attaches the decal on the specified marker. use "self" if attaching it to the unit itself
    EXAMPLE: basePosition: self

  basePositionFromLegEnd                                leg/arm              1.15p9   Attaches the decal to the specified leg or arm ending.
    EXAMPLE: basePositionFromLeg: leg_3, basePositionFromLeg: arm_5

  basePositionFromTurret                                turret               1.15p9   Attaches the decal to the specified turret
    EXAMPLE: basePositionFromTurret: rocketLauncherBase

  # Waypoint Lines #                                                         1.15p9   

  drawLineTo                                            marker ref           1.15p9   Draws a line from the unit to the specified marker, useful for custom waypoints
    EXAMPLE: drawLineTo: attacking.customTarget2

  color                                                 color hex            1.15p9   Sets the color of the drawn line
    EXAMPLE: color: #ffff00

  lineWidth                                             float                1.15p9   Sets the width of the drawn line
    EXAMPLE: lineWidth: 2


====================================================================================================
SECTION: [comment_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  List of default game noises (for turret fire sound or other sound references):                               bug_attack, bug_die, building_explode, cannon_firing, click, click_add, click_remove, firing3, firing4, gun_fire, interface_error, large_gun_fire1, large_gun_fire2, laser_deflect, laser_deflect2, lighting_burst, message, missile_fire, missile_hit, move, nuke_explode, nuke_launch, plasma_fire, plasma_fire2, tank_firing, unit_explode, unit_explode_old, warning


====================================================================================================
SECTION: [template_NAME]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  @copyFromSection                                                           1.13.3   Use in any section to include keys from a section or template. (Comma separated for multiple)
    EXAMPLE: @copyFromSection: template_name/action_name/projectile_name

  @copyFrom_skipThisSection                                                           Use in any section to make [core]copyFrom not copy into it. Eg not copy an action when overriding
    EXAMPLE: @copyFrom_skipThisSection

  @define X                                                                           Define a local variable within a section (best outside of template)
    EXAMPLE: @define targetEffect: boom

  @global X                                                                           Define a global variable used in all sections. Local variables have a higher priority
    EXAMPLE: @global targetEffect: pop

  @memory X                                                                  1.15     Define a memory for this unit, its type must be defined as well.
    EXAMPLE: @memory transportCount: float

  File                                                  NAME.template                 A template that can be used to share many parts across units, used with copyFrom:                                       However, cannot define variables


====================================================================================================
SECTION: [decal]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  File                                                  all-units.template            Put at the root of the mod folder, applies anything in it to ALL units across all .ini files so that you have "common characteristics", can have any sections but cannot define local variables. Can define memories though.


====================================================================================================
SECTION: [action_spawnedInitialize]
====================================================================================================
CODE                                                    TYPE                 SINCE    DESCRIPTION
----------------------------------------------------------------------------------------------------
  File                                                  mod-info.txt                  This file defines some of properties of a mod for display in mod browser and in Steam Workshop. It should be placed in the top of mod's file hierarchy, otherwise will result in error

  Key                                                   Value type           Section  Description
    EXAMPLE: Example

  title                                                 String                        Sets the title of the mod package
    EXAMPLE: title: Sample mod

  description                                           String                        Sets the description of the mod package. Doesn't support line breaks.
    EXAMPLE: description: This is a sample of mod that samples a unit to the game

  tags                                                  String                        Sets varied tags depending on what is specified, can be multiple with comma as separator
    EXAMPLE: tags: units, sample, demo

  minVersion                                            String                        Declares the minimum version compatible for the mod. It is important to declare one when using decals and other modding keys starting on version 1.15p9 beta
    EXAMPLE: minVersion: 1.15p9

  thumbnail                                             image                         Sets the mod's thumbnail in Steam Workshop
    EXAMPLE: thumbnail: assets/images/other/thumb.png

  id                                                    string               1.16     Can be set to anything. The ID to refer to for dependency of other mods

  requiredMods                                          string/id list       1.16     What mod(s) required to use this mod, for stuff like dependent "DLC" expansions

  requiredModsMessage                                   string               1.16     The message that displays if you do not have the mod necessary

  sourceFolder                                          audio dir                     Sets where the custom music will be played for the mod
    EXAMPLE: sourceFolder: assets/audio/music

  whenUsingUnitsFromThisMod_playExclusively             bool                          Only plays music from this mod
    EXAMPLE: whenUsingUnitsFromThisMod_playExclusively: true

  Projectile Draw Types                                                               in [projectile_#], a unit may use a built-in frame from the res/drawable directory. However, only three strips are designated for this use case. Use the id value for the drawType key, and a frame number for a desired appearance.
    EXAMPLE: If there is no image or frame supplied, the game will display a white dot instead, wherein a modder can change its color using the color key. This is observable to Command Center, Heavy Tank, and Missile Tank

  name                                                  usable frames        id       image (with frame numbers)

  projectiles.png                                       12                   0        

  projectiles_large.png                                 3                    1        

  projectiles2.png                                      6                    2        
